
Changelog (v3.45.1)
* mission_stream.js: Converted pacing from continuous stream to a 3-round structure.
    * Injected a 4000ms pause before each phase.
    * Implemented Sound 192 (Resonance Cascade) as the phase-completion tone using the native AudioContext API (plays before phases 2 and 3).
    * Integrated new `_snu` voice assets (`phase_1_snu`, `phase_2_snu`, `phase_3_snu`) to announce the rounds during the break.
    * Offset the `startTime` during pauses to prevent the downtime from corrupting the `checkGameState` math.
    * Updated `stop()` to clear the new phase transition timer, adhering to Sentinel S3 (Lifecycle Resets).
Changelog (v3.45)
* mission_stream.js: Removed the dynamic pacing engine.
    * The drop rate of characters is now strictly locked to the `initialSpeed` determined by the mission's difficulty setting. 
    * The game no longer speeds up or slows down between phases based on player reaction time, ensuring a predictable and standardized training environment.
Changelog (v3.43.2)
* mission_launch.js: Amplified the "Recall Mode" ready cue.
    * Artificially boosted the gain of the `synth_23` alert by stacking multiple synchronous playback calls, bypassing the HTML5 1.0 volume ceiling to ensure the cue cuts clearly over ongoing TTS announcements.
Changelog (v3.43.1)
* mission_launch.js: Corrected Sentinel S1 violation. Replaced the blacklist state guard in `handleInput` with a strict whitelist (`TYPING` or `DICTATING`).
* kc_mission_core.js: Corrected missed version header bump from the previous update cycle.
Changelog (v3.43)
* mission_launch.js: Introduced "Recall Mode" vs "Shadow Mode" dual functionality.
    * Shadow Mode: Retains current behavior (Type-Along allowed during dictation).
    * Recall Mode: Implements strict working-memory training.
        * Plays `synth_23` cue at the end of dictation to signal clearance to type.
        * Enforces an "Early Launch" penalty if the user types during dictation (-10s time, verbal warning, and input buffer wipe).
    * Mission Setup UI: Added a new dedicated row to toggle between Shadow and Recall modes without overwriting existing settings.
Changelog (v3.42 - Batch Completion)
* Global Mission Audit: Resolved Sentinel S4 (Ghost Keys) violation across all remaining active missions.
    * Injected `if (e.repeat) return;` at the top of the `handleInput` function in Stream, Race, Reflex, and Echo Chamber missions to prevent the OS-level auto-repeat feature from flooding the input buffer.
Changelog (v3.42.1)
* mission_launch.js: Resolved Sentinel S4 (Ghost Keys) violation.
    * Injected `if (e.repeat) return;` at the top of the `handleInput` function to prevent the OS-level auto-repeat feature from flooding the input trap when a key is held down.
Changelog (v3.41)
* kc_input.js: Resolved Sentinel S6 (Hub Audio Killswitch) violation.
    * Removed rogue global `Escape` key intercept that was prematurely hijacking input and bypassing state-specific cleanup routines.
    * Injected explicit `KC.audio.stopIntro()` and `KC.audio.stopActiveAudio()` killswitches into the `ACTIVE_TYPING` and `MISSION_START` state routers.
    * Restored localized `Escape` handling authority to individual menus and mission states.
Changelog (v3.40.4)

* mission_launch.js: Initiated architectural refactor to resolve "Swallowed Keystrokes" for expert typists.
    * Action: Handoff to IDE AI to implement "Type-Along" capability.
    * Goal: Remove the strict input lockout during the "DICTATING" state, allowing the engine to instantly evaluate and advance the `currentIndex` even while the sequential audio callouts are still playing in the background.
Changelog (v3.40.3)
* mission_launch.js: Adapted the v3.40.2 diagnostic payload for Screen Reader accessibility.
    * Changed the diagnostic trigger hotkey from F2 (which was globally blocked) to PageUp.
    * Rewrote `dumpDiag` to inject an accessible `<textarea>` into the DOM, populate it with the JSON log, and automatically apply `.focus()` and `.select()` for immediate clipboard extraction.
Changelog (v3.40.2)
* mission_launch.js: Implemented a forensic diagnostic logger to isolate audio bank mismatches and state desyncs.
    * Created an internal circular buffer to track critical event lifecycles.
    * Added `GENERATE` logging to track the full target string.
    * Added `DICTATE_EMIT` logging to capture the exact character the engine intends to speak versus the exact file path `getCharAudio` resolves for it.
    * Added `KEY_COMPARE` logging to track the user's raw keystroke against the engine's current expected character and index.
    * Wired the `F2` key to dump the accumulated diagnostic JSON directly to the browser console for external review.
Changelog (v3.40)
* System: Version bumped to v3.40 to establish a stable baseline.
* Diagnostics: Concluded internal investigation of the `mission_launch.js` phantom input bug. 
* Findings: Isolated the root cause to a "State Poisoning / Index Desync" loop that fails to reset core variables after dropping into the FAILED state. 
* Action: Handoff initiated to VS Code IDE for deep-state variable tracking and patch deployment. No codebase files altered in this cycle.
Changelog (v3.39.5)
* kc_audio.js: Integrated "Alert Double-Beep" (Option 16 from Soundtester) into the playSynth switch registry as Synth ID 16.
* mission_launch.js: 
    * Fixed the auditory hallucination bug in `startTimer`. Replaced the ambiguous 'click' with Synth 16, firing specifically at 15s, 10s, and a final 5-to-1 second countdown.
    * Enhanced the failure TTS announcement in `handleInput` to explicitly state the expected character versus the received character for precise diagnostic clarity.
Changelog (v3.39.4)
* mission_launch.js: 
    * Implemented an `ariaToggle` state to alternate success messages ("Code accepted" vs. "Code verified"), guaranteeing a DOM mutation to reliably trigger the ARIA TTS.
    * Injected a diagnostic tracker into `handleInput` that audibly announces if the engine receives two identical keystrokes in under 60ms, without blocking the inputs.
Changelog (v3.39.3)
* kc_audio.js: Version bumped to v3.39.3 to ensure synchronization with mission logic.
* mission_launch.js: 
    * Injected 150ms into the stagger delays: Shot 2 is now at 300ms, and Shot 3 is at 700ms total offset.
    * Widened the stereo field: Shot 2 panned to 60% Left (-0.6) and Shot 3 panned to 75% Right (0.75).
Changelog (v3.39.2)
* [cite_start]kc_audio.js: Updated playPannedSFX to include a try/catch fallback to handle browser MediaElementSource node limits and prevent silent failures. 
* [cite_start]mission_launch.js: Increased temporal stagger between shots to 150ms and 250ms (400ms total) to allow for stable browser audio initialization.
Changelog (v3.39.1)
* kc_data.js: Standardized explosion asset keys to 'explode_1' through 'explode_7' to fix dictionary lookup failures.
* mission_launch.js: Verified and locked the endMission logic to use the standardized 'explode_' naming convention for the triple-staggered panned sequence.
Changelog (v3.39.0)
* kc_audio.js: Implemented `playPannedSFX` using StereoPannerNode to allow precise coordinate-based audio placement for neuroplasticity training.
* mission_launch.js: 
    * Relocated 'powerup' SFX to `start()` to serve as a mission-start indicator alongside TTS.
    * Implemented triple-staggered explosion sequence in `endMission()` with randomized asset selection and specific temporal offsets (50ms/75ms).
    * Integrated spatial panning for the finale: Shot 1 (Center), Shot 2 (40% Left), Shot 3 (60% Right).
    * Added 4000ms delay to the final After Action Report to accommodate the new audio sequence.
Changelog (v3.38.0)
* kc_data.js: Injected 18 new 8-bit performance stings (Triumph, Neutral, Sadness) into the audio bank.
* mission_stream.js: Integrated 8-bit musical stings into `endMission` and `triggerOverflow`. Added a 4000ms delay to the victory sequence to ensure the music plays in the clear.
Changelog (v3.37.0)
* kc_core.js: Updated profile generation and loading logic to establish "Belle" as the default `preferred_voice`, while adding a `mission_voices` dictionary to track custom overrides.
* kc_hub.js: Injected the "Tactical Voice" setting into the Sound and Sight Settings menu, supporting "Amelia", "Belle", and "Per-Mission".
* kc_input.js: Expanded the Settings menu input boundaries to 5 rows and mapped the left/right arrow keys to save the new global voice preference.
* kc_mission_core.js: Updated `loadLesson` to dynamically inherit the global or per-mission voice preference. Updated `executeMission` to silently save the user's active choice if the "Per-Mission" global setting is enabled.
Changelog (v3.36.0)
* mission_stream.js: Updated the `speakTarget` function to support Tactical Voice routing. Added translation logic to ensure Belle correctly calls out numbers and specific symbols when active, while maintaining Amelia's legacy alias mapping.Changelog (v3.35.0)
* mission_race.js: Injected dynamic voice routing into the `spellWord` function. When Belle is the active Tactical Voice, she replaces Amelia for the individual letter spelling callouts.
Changelog (v3.34.5)
* kc_data.js: Corrected spelling typos in the `audio_bank` for Belle's ampersand, carat, and decimal assets to match the newly corrected local filenames, ensuring proper audio routing.
Changelog (v3.34.4)
* mission_launch.js: Corrected the spelling in Belle's `symMapSnu` dictionary, mapping the right parenthesis to `sym_closed_parenthesis` to ensure proper routing to the local audio asset.
Changelog (v3.34.2)
* mission_launch.js: Implemented dynamic audio sequence timing. Letters and numbers now strictly allocate 500ms, while symbols allocate 1000ms to accommodate longer TTS phonetic translations.
Changelog (v3.34.1)
* mission_launch.js: Fixed a race condition where double-tapping the Spacebar during mission initialization caused overlapping, simultaneous audio sequences.
Changelog (v3.34.0)
* kc_data.js: Injected 86 new "Southern Neutral" (SNU) voice assets into the `audio_bank`.
* kc_mission_core.js: Added a new "Tactical Voice" (Amelia / Belle) toggle to the mission setup screen, saving the preference to `KC.state.missionParams.voice`.
* mission_launch.js: Rewrote `getCharAudio` to dynamically route character announcements to Belle's `_snu` dictionary keys when selected, while falling back to Amelia's default paths.
Changelog (v3.33.0)
* mission_launch.js: Increased the initial audio broadcast delay by an additional 200ms for extra screen reader TTS clearance.
* mission_launch.js: Increased the intra-character delay by an additional 50ms to further improve phonetic spacing on long launch codes. Changelog (v3.32.0)
* kc_data.js: Removed non-functional placeholder missions ("Password Decryption" and duplicate "Reflex Calibration") from the Arcade submenu, leaving "Launch Codes" as the standalone combat training option.
Changelog (v3.31.0)
* mission_launch.js: Added an additional +200ms delay to the initial launch code broadcast for wider screen reader TTS clearance.
* kc_mission_core.js: Fixed a BGM routing bug where mission music reverted to "arcade" upon starting a mission by injecting an ambient map lookup.
* kc_mission_core.js: Fixed a UI bug where exiting the mission report screen kicked users to the Hub by explicitly resetting `setupCursor` to 0.
Changelog (v3.30.0)
* Adjusted Launch mission audio timing: added a +300ms delay to the initial code broadcast to prevent overlap with native screen reader TTS queues.
* Increased the delay between individual character callouts in Launch codes by +100ms to improve audio clarity on longer 6+ digit sequences.
Changelog (v3.29.1)
* Smart Audio Pathing: Injected a dynamic path resolver into the BGM engine. Tracks missing from the `GAME_DATA.audio_bank` dictionary will now automatically resolve to `"audio/music/[track_id].mp3"`, preventing silence when loading new mission ambient sets.
Changelog (v3.29.0)
* Ambient Setup BGM: Added mission-specific audio arrays (`bmu_data`, `bmu_keyboard`, `bmu_systems`, `bmu_launch`) to the BGM playlists.
* Dynamic Setup Audio: Entering a Mission Setup screen now triggers an immediate crossfade into the mission's dedicated ambient track grab bag.
* Hub Return: Backing out of a mission setup via Escape automatically reverts the BGM back to the Cadet's preferred profile setting.Changelog (v3.28.0)
* BGM Engine Refactor: Removed the 3.5-second volume-ducking preview logic from the Sound & Sight menu. 
* Hard Cut Transitions: Selecting a new music style now executes an immediate hard cut to a fresh track drawn from the newly selected style's randomized grab bag.
Changelog (v3.27.4)
* Audio Lifecycle Targeting: Corrected an injection error where the intro audio killswitch was placed in the Settings Menu instead of the Mission Setup menu. Pressing Escape during Mission Setup will now properly terminate the intro audio.
Changelog (v3.27.3)
* Audio Lifecycle: Fixed a bug where mission intro MP3s would continue playing after returning to the Hub.
* Input Intercept: Added an explicit `KC.audio.stopIntro()` call to the Escape key listener within the Mission Setup state.
Changelog (v3.27.2)
* UI Restoration: Re-injected the navigation instructions (`[Up/Down: Navigate | Left/Right: Adjust | Enter: Start | Esc: Cancel]`) into the Mission Setup HTML footer.
* TTS Instructions: The initial setup announcement now correctly reads out the Arrow, Enter, and Escape key instructions for visually impaired Cadets.
* BGM Pipeline Restored: Re-wired `KC.bgm.switchToStyle()` into the `executeMission` function. Missions will now automatically crossfade to their designated music style (defaulting to 'arcade' for action missions).
* Hub BGM Restoration: Re-wired `KC.bgm.restoreStyle()` into `KC.hub.enterHub()` to ensure the music smoothly transitions back to the Cadet's preferred profile theme when they leave a mission.
Changelog (v3.27.1)
* Intro TTS Synchronization: The initial mission setup TTS announcement is now delayed and safely chained to the `.onended` event of the Intro Audio node. It will only play after Amelia finishes speaking.
* Audio Killswitch Callbacks: Pressing 'X' while the intro is playing instantly halts the audio and forces the delayed TTS announcement to fire immediately (prepended with "Intro Audio Disabled").
* Navigation Override: If a Cadet starts navigating the menu (Arrow Keys) while the intro is still playing, the engine safely aborts the delayed initial announcement so it doesn't randomly interrupt their navigation 20 seconds later.
* FTUE Feedback: Re-appended "Press X to toggle intro message" to the end of the initial TTS block so returning users are always reminded of the feature.
Changelog (v3.27.0)
* Intro Audio Restored: Re-wired the Mission Setup screen to support `audio_briefing` MP3s. Intros use a dedicated HTML5 Audio node so they do not interrupt system TTS.
* Dynamic Audio Mapping: The engine now dynamically maps missing `audio_briefing` paths (`systems.mp3`, `keyboard.mp3`, `data.mp3`, `launch.mp3`) based on mission ID, bypassing the need to overwrite the `GAME_DATA` master file.
* Profile Memory: The 'X' key toggle state is now saved persistently to `KC.state.profile.disabled_intros` on a per-mission basis.
* TTS Feedback: Pressing 'X' announces "Intro Audio Disabled" or "Intro Audio Enabled" for immediate screen reader feedback.
* Audio Killswitch: The dedicated intro node is instantly killed if the user presses `Enter` to start the mission or `Escape` to return to the Hub.
Changelog (v3.26.2)
* Timer Fix: Flushed the `timerInterval` cache to `null` during initialization to prevent subsequent playthroughs from skipping the master clock activation.
* Telemetry: Added a 15-second audio indicator (`click` sound) to the `startTimer` interval to passively alert the player to the passing time.
* Penalty Logic: ArrowDown time penalty now instantly ends the mission if the 10-second deduction drops the `timeRemaining` to zero or below, rather than waiting for the next clock tick.
* Audio Abort: `playAudioSequence` now strictly aborts all state changes if `isActive` flips to false mid-dictation.
Changelog (v3.26.1)
* Audio Routing Fix: Corrected the alpha character dictionary mapping in `mission_launch.js`. Letters are now correctly routed to the `/audio/alpha/` directory using the `char_[x]_ame` naming convention, restoring Amelia's voice for alpha sequences.
Changelog (v3.26.0)
* Pacing: Added a 500ms initial pause to `dictateCode` so Cadets have a half-second to mentally prep before Amelia reads the first character.
* Feedback: Added a 'powerup' audio cue and "Time expired" TTS announcement when the mission timer hits zero.
* Progression: Wired Launch Codes into the game's economy. The mission now calculates Accuracy, Keys, and Errors, and converts your Final Score into Data Blocks, automatically saving them to your Profile.
* Navigation Fix: The `endMission` function now explicitly sets `KC.mission.activeHandler = null`, relinquishing keyboard control back to the core engine so the Up/Down arrows correctly read the line-by-line summary.Changelog (v3.25.4)
* Version Correction: Re-stamped recent Launch Codes updates from 4.x back to the 3.25.x branch.
* Audio Execution Fix: Bypassed the audio_bank registry in favor of raw HTML5 Audio objects. The engine now explicitly appends `.mp3` to the directory paths to guarantee Amelia's voice assets play.
* Summary Screen Crash Fix: Removed the forced 3-second `renderMenu()` timeout from `endMission()`. The mission now natively pipes stats into `KC.state.reportLines` and shifts the engine to the `REPORT` state, allowing indefinite line-by-line review.Changelog (3.25.4)
* Audio Execution Fix: Bypassed the audio_bank registry in favor of raw HTML5 Audio objects. The engine now explicitly appends `.mp3` to the directory paths to guarantee Amelia's voice assets play.
* Summary Screen Crash Fix: Removed the forced 3-second `renderMenu()` timeout from `endMission()`. The mission now natively pipes stats into `KC.state.reportLines` and shifts the engine to the `REPORT` state, allowing indefinite line-by-line review.

Changelog (v4.25.3)
* Audio Routing Fix: Corrected the `getCharAudio` lookup table in Launch Codes. The engine now properly prepends the `audio/numbers/`, `audio/letters/`, and `audio/symbols/` directory structures to the keys, successfully resolving the recorded Amelia voice assets instead of falling back to TTS.
Changelog (v4.25.2)
* Mission Mechanics Overhaul: Launch Codes now evaluates keystrokes instantly (no Backspace correction).
* Progression Loop: Spacebar is now used exclusively to trigger the dictation of the *next* code after a sequence completes or fails.
* Audio Pacing: Reduced the dictation intervals from 700ms to 400ms for faster, more challenging sequence readouts.
* Voice Assets: Re-mapped the audio dictation sequence to accurately target Amelia's recorded letter (`let_x_am`) and number (`num_x_am`) assets instead of relying on generic TTS.
* Feedback: Added discrete audio cues per keystroke (`click` for correct key, `powerup` for sequence complete, `error` for incorrect key/failure).
Changelog (v3.25.0)
* New Mission Engine: Added `mission_launch.js` (Launch Codes).
* Mechanics: Audio-dictated string retention, partial-credit scoring, spacebar submission.
* Risk/Reward: Pressing Down Arrow repeats the audio code but deducts 10 seconds from the global timer.
* Dynamic Hub UI: The Hub now locks the Hand Selector to "Both" and dynamically injects a "Code Length (4-12)" row when Launch Codes is selected.
* Zone Dictionaries: Added 6 distinct target zones ranging from isolated Numpad/NumRow strictly enforced via `event.code` to complex Alpha-Symbol hybrids.
Changelog (v3.24.0)
* UX Overhaul: Introduced the "Sound and Sight Settings" menu in the Hub.
* Visuals: Added 8 low-vision high-contrast themes and 4 stepped font-size increments (100% to 200%).
* Audio: Integrated BGM style previews with "Deep Duck" volume logic (5% background level during preview).
* Persistence: Settings are now saved directly to the user profile and persist across sessions.Changelog (v3.23.1)
* Audio Registry (`kc_data.js`): Registered the legacy BGM tracks (`mu_default1-4` and `mu_spaghetti1-4`) into the `audio_bank` to align with the new key-based playlist architecture introduced in v3.23.0, preventing silent failures during track shuffling.Changelog (v3.23.0)
* BGM Engine (`kc_data.js`, `kc_bgm.js`): Expanded the ambient background music playlists. The `default` and `spaghetti` styles have been expanded from 4 tracks to 8 tracks each. Introduced a new 8-track `arcade` style playlist for use in future Hub interfaces.
Changelog (v3.22.0)
* Audio Registry (`kc_data.js`): Mapped the 17 Commander Keyboard hesitation keys (`ck_mut_hmm1`, etc.) to their physical file equivalents in the newly established `/audio/char/` directory, restoring his AI typing hesitation SFX.
Changelog (v3.21.4)
* Word Generator Rescue (`kc_mission_core.js`): Restored the universal vowel injection (`aeiouy`) to the `regionsRace` arrays. This ensures that restricting gameplay to a single hand (like the Right Hand on the Home/Bottom row) will not result in an empty word pool. Restored the explicit `(+Vowels)` tags to the UI setup menu.
Changelog (v3.21.3)
* Engine Logic (`kc_mission_core.js`): Fixed a critical validation bypass in `isWordInZone`. The word generator now correctly pulls the `left`, `right`, or `both` character arrays based on the player's selected Hand Mode (`reflexMode`), rather than strictly defaulting to `both` for all words.Changelog (v3.21.2)
* Engine Routing (`mission_race.js`): Fixed a legacy routing bug where the word generator was passing an integer instead of the `"race"` string to `isWordInZone`. This ensures the Race mission correctly pulls from the expanded `regionsRace` array, preventing empty pools and immediate mission failures on Expert settings.
Changelog (v3.21.1)
* UI / Architecture (`kc_mission_core.js`): Expanded the `regionsRace` array from 4 to 7 options. Explicitly added "Expert" variations for the Top/Home, Bottom/Home, and All Alpha quadrants so they visibly render in the mission setup menu.
Changelog (v3.21.0)
* Architecture Realignment (`kc_mission_core.js`): Decoupled word length from the `difficulty` parameter inside the `isWordInZone` generator. Word length is now strictly controlled by the quadrant's `isExpert` flag, allowing Cadets to race a fast AI on easy words, or a slow AI on expert words.
* Quadrant Restoration (`kc_mission_core.js`): Restored `isExpert: true` to the "All Alpha" quadrants to re-enable long-form word generation.
Changelog (v3.20.0)
* Audio Expansion (`kc_bgm.js`): Added `data` and `keyboard` arrays to the BGM `playlists` object, mapping 8 tracks for each of the new mission styles.
* Contextual Routing (`kc_mission_core.js`): Expanded the `loadLesson` BGM check to route `stream` generators to the `"data"` style and `race` generators to the `"keyboard"` style, utilizing the seamless crossfade architecture established in v3.19.Changelog (v3.19.0)
* Audio Engine (`kc_bgm.js`): Added the `systems` playlist with all 8 new tracks. Introduced `switchToStyle()` for seamless transition and `restoreStyle()` to return to user preferences.
* Mission Routing (`kc_mission_core.js`): Updated `loadLesson` to automatically trigger the `systems` style when loading a `reflex` mission.
* Hub Routing (`kc_hub.js`): Updated `enterHub` to automatically restore the user's preferred music style when exiting a mission.
n
Changelog (v3.17.0)
* Curriculum Purge (`kc_mission_core.js`): Permanently removed the "Function Keys" quadrant. F-keys (especially F5, F6, F12) trigger un-overrideable browser behaviors and cannot be safely used as target inputs. The curriculum is now officially a 9-Quadrant board.
* UI/UX Streamlining (`kc_input.js` & `kc_mission_core.js`): Overhauled the Mission Setup menu UX. Pressing `Enter` while highlighting any setting row will now instantly launch the mission, bypassing the need to navigate down to the explicit "Start Mission" row. `Left/Right` arrows remain the exclusive method for adjusting setting values.
* Documentation (`rules.md`): Updated Section 2 to reflect the 9-Quadrant progression.
Changelog (v3.16.0)
* Curriculum Stabilization (`kc_mission_core.js`): Renamed the 10th quadrant from "Modifiers & Navigation" to "Navigation & Editing". 
* Accessibility Hotfix (`kc_mission_core.js`): Permanently purged system-level keys (`Escape`, `Tab`, `CapsLock`, `Shift`, `Control`, `Alt`) from the curriculum arrays. These keys trigger inescapable OS/Screen Reader behaviors (muting TTS, losing focus, triggering browser menus) and cannot be safely tested as raw inputs in a web environment.
* Documentation (`rules.md`): Updated Section 2 to reflect the renamed quadrant and document the ban on testing state/modifier keys.
Changelog (v3.15.0)
* Input Stabilization (`kc_input.js`): Removed the legacy "Quick Start" mission hotkey from the Hub root. Pressing `Enter` in the HUB state now exclusively opens the Deck Menu, eliminating accidental double-bounce mission launches when navigating the login screen.
* Engine Stability (`kc_mission_core.js`): Hardened the `executeMission` launch sequence. Triggering a legacy/deprecated mission (e.g., D00-01) that lacks a valid modern engine handler will no longer hard-crash the game; it will now gracefully announce the error and route the user back to the Hub.
* Documentation (`rules.md`): Updated Section 5 (Input & Navigation Standards) to explicitly prohibit hidden quick-start hotkeys at the Hub level to prevent UI double-bounces.
Changelog (v3.14.6)
* Economy Architecture (`kc_core.js`): Migrated the profile economy from a flat `currency` variable to a `wallet` object (`credits`, `dataBlocks`, `components`) to support the upcoming Fabricator facility. The Dev Console now provisions 10,000 to all wallet properties simultaneously.
* Environment Security (`kc_core.js`): Gated the Dev Console. The "Stay Out" Hub menu option is now dynamically rendered and will only appear if the active profile's callsign is strictly "BRIAN" or contains the substring "bot" (case-insensitive).
* Documentation (`rules.md`): Updated Section 4 to document the new profile-name security requirement for Developer Operations.Changelog (v3.14.5)
* UI/UX Accessibility Hybrid (`kc_core.js`): Restored the visual DOM overlay (green-on-black terminal aesthetic) for the Dev Console while retaining the accessible `KC.core.announce()` audio pipeline. Sighted users get the visual interface, and screen readers get proper focus and audio cues simultaneously.
Changelog (v3.14.3)
* UI/UX Accessibility (`kc_core.js`): Abandoned unreliable keyboard chord hooks for the Dev Console. Added a dedicated "Stay Out" option to the main Hub menu (positioned above Save & Exit) to ensure 100% accessible, environment-proof entry into the developer tools.
* Narrative Easter Egg (`kc_core.js`): Added an ominous audio/text warning ("Abandon all hope, yee who enter.") when triggering the Dev Console via the Hub.
* Documentation (`rules.md`): Updated Section 4 to reflect that Dev Operations are now accessed via the Hub UI, not a hidden hotkey.
Changelog (v3.14.2)
* Input Stabilization (`kc_input.js`): Permanently migrated the Dev Console hotkey to `Ctrl + Shift + X` (an alpha-chord) to bypass all un-overrideable browser and screen reader conflicts associated with Function and Symbol keys.
* Quadrant Lockdown (`kc_mission_core.js`): Permanently banned the Tilde (~) and Backtick (`) characters from the "Punctuation & Brackets" quadrant. These characters trigger inescapable environment terminals and cannot be safely tested in the curriculum.
* Documentation (`rules.md`): Updated Section 4 to officially document the `Ctrl + Shift + X` standard and the Tilde/Backtick ban.
Changelog (v3.14.1)
* Input Stabilization (`kc_input.js`): Migrated the Dev Console hotkey from Tilde (~) to `Ctrl + Shift + F2` to prevent unavoidable focus-stealing conflicts with browser UI and screen reader form modes.
* Quadrant Restoration (`kc_mission_core.js`): Restored the Tilde (~) and Backtick (`) characters to the "Punctuation & Brackets" quadrant. The game engine now actively claims these keys again, preventing the environment from dumping the user into hidden text fields during missions.
* Documentation (`rules.md`): Updated Section 4 to reflect the new `Ctrl + Shift + F2` Dev Console standard.
Changelog (v3.14)
* Developer Operations (`kc_core.js`): Implemented a globally accessible, headless-friendly 2D Dev Console with a classic terminal aesthetic (green-on-black). Added actions for Warp, Unlock All, Currency Injection (10,000 Credits), EXP Boost (Placeholder), and Save Wipe.
* Hotkey Quarantine (`rules.md` & `kc_input.js`): Stripped all legacy dev hotkeys. The Dev Console is now exclusively bound to the Tilde (`~` / `` ` ``) key. 
* Quadrant Integrity (`kc_mission_core.js`): Removed the Tilde and Backtick characters from the "Punctuation & Brackets" quadrant to ensure the Dev Console key is never requested as a valid mission input.
Changelog (v3.13)
* Curriculum Expansion (`rules.md`): Officially documented the 10-Quadrant learning path (Alpha, Data, and Control groups) as the new project standard.
* Audio Mapping (`kc_data.js`): Added an `AUDIO_DICT` lookup table to bridge physical keystrokes (like '!' or 'F1') directly to their newly discovered `_am` synthesized audio files.
* Mission Architecture (`kc_mission_core.js`): Decoupled region selection so Keyboard Commander safely serves only the 4 Alpha Quadrants, while System Override and Data Dump serve the complete 10-Quadrant comprehensive array.
Changelog (v3.12)
* Watchdog Directive (rules.md): Codified the 2D Menu Navigation standard (Up/Down to select category, Left/Right to adjust values).
* UI Overhaul (kc_mission_core.js): Replaced the button-based mission setup with a 2D dynamic grid. Added "Exit to Deck" as a focusable menu item.
* Visual Styling (kc_hub.js & css): Implemented mission-specific theme classes (.theme-race, .theme-reflex, .theme-stream) to provide high-contrast background shifts for sighted instructors.
* Input Logic (kc_input.js): Removed legacy 1-6 and PageUp/Down hotkeys in favor of the new 2D directional setup logic.
Changelog (v3.10.1 Patch)
* Mission Routing Fix (`kc_mission_core.js`): Fixed a critical crash where the `executeMission` router was missing the conditional logic to launch `reflex` (System Override) and `echoc` mission handlers.
Changelog (v3.10)
* Region Expansion (`kc_mission_core.js`): Added an "All Alpha (Top, Home, Bottom)" region to both the Race and Reflex region arrays. This enables full-keyboard training (Left, Right, and Both hands) across all three mission types (Keyboard Commander, System Override, and Data Dump) while maintaining compatibility with the existing dynamic word generation filters.
Changelog (v3.9.1 Patch)
* Architecture Fix (`kc_core.js`, `kc_hub.js`, `kc_input.js`): Fixed a critical crash on profile boot. Updated the profile data schema to track `currentLessonID` (a string) instead of `currentLessonIndex` (an integer), correctly aligning the save data with the new string-based dictionary architecture in `kc_data.js`. Added a migration patch to repair existing corrupted saves.
Changelog (v3.9)
* Watchdog Directive (`rules.md`): Updated Section 4 to codify the "Visual Echo" rule, ensuring any free-form text input fields (like name entry) dynamically mirror the hidden `inputTrap.value` to the display text.
* UI Fix (`kc_input.js`): Added a micro-timeout to the `LOGIN_INPUT` state. The system now dynamically renders the user's keystrokes (with a cursor) onto the display screen in real-time while they create their callsign. Added `Escape` to back out of creation.
Changelog (v3.8.1 Patch)
* Watchdog Directive (`rules.md`): Updated Section 2 to codify the Background Music (BGM) Engine rules, including the dual-element HTML5 crossfader logic, user profile persistence for settings, and global hotkeys.
* Bug Fix (`kc_bgm.js`): Fixed incorrect directory pathing for background music files. Removed the `assets/` prefix so the engine correctly targets the `audio/music/` directory.
Changelog (v3.8)
* Background Music Engine (`kc_bgm.js`): Ported the native `audioA`/`audioB` HTML5 crossfader and "grab bag" shuffling logic from Accessible Bible.
* Profile Integration (`kc_core.js`, `kc_hub.js`): Added a `settings` object to user profiles to save global volume and music style. The BGM engine initializes at Volume 10 on the Login screen and dynamically crossfades/adjusts volume when a user selects their profile.
* Global Audio Controls (`kc_input.js`): Added `Shift + V` to cycle volume levels (0, 5, 10, 20, 30, 40) and `Shift + M` to cycle music styles (Default, Dusty Western).Changelog (v3.7)
* Watchdog Directive (rules.md): Added UI standards for "2D Data Tables" to Section 4, ensuring all data-heavy displays use the XY axis (Arrows) for navigation and clear audio indexing.
* Archive Overhaul (kc_hub.js): Replaced placeholder career stats with a dynamic 2D Data Engine.
    - Tab 1: Recent History (Scroll through the last 50 missions with Date, ID, WPM, and Accuracy).
    - Tab 2: Zone Mastery (Aggregated lifetime stats for Home Row, Top Row, etc.).
* Navigation Logic (kc_input.js): Integrated XY navigation for the Archive state, allowing Left/Right to switch categories and Up/Down to traverse data entries.
Changelog (v3.6.1 Patch)
* Bug Fix (`kc_mission_core.js`): Fixed a Reference Error in the Data Engine logging block. Updated `wpm` to `transmissionRate` and `accuracyPct` to `acc` to match the local function parameters, restoring the mission report card sequence.Changelog (v3.6)
* Watchdog Directive (`rules.md`): Updated Section 3 (Economy & Persistence) to codify the Multi-User Roster architecture and the Data Engine schema (Rolling Buffers and Zone Stats). Added the `location.reload()` standard for hard resets to Section 4.
* Data Engine (`kc_core.js`): Expanded the `career` object schema to include `history_buffer` and `zone_stats`. Added a migration patch to `loadProfile` to safely upgrade legacy profile saves without crashing.
* Mission Logging (`kc_mission_core.js`): Intercepted `showReportCard` to automatically push mission results (Date, ID, WPM, Accuracy, Grade, Region) into the player's 50-mission rolling buffer.
* Quadrant Tracking (`kc_mission_core.js`): The engine now tracks cumulative missions, WPM, and accuracy per specific keyboard region (e.g., "Home Row", "Top Row") to allow for localized weakness detection.Changelog (v3.5)
* Boot Audio & Navigation (`kc_hub.js`): Updated the initial Login screen announcement to provide a clearer, more welcoming orientation message. 
* System Exit (`kc_hub.js`, `kc_input.js`): Added an "Exit" option to the bottom of the Profile Select roster. Selecting it triggers a `location.reload()` to act as a hard kill-switch for all game state and audio, safely returning the user to the initial browser splash screen.
* Deletion Safety (`kc_input.js`): Protected the new "Exit" option from being accidentally triggered by the `Delete` key.Changelog (v3.3.2 QoL)
* Input Pass-through (`kc_input.js`): Added a Browser Defaults Whitelist to the top of the global key handler. F-keys (F1-F12) and Ctrl+R are no longer swallowed by `preventDefault()`, restoring native refresh (F5/Ctrl+F5) and frame navigation (F6).
* Profile Management (`kc_input.js`, `kc_core.js`): Users can now press the `Delete` key while highlighting a profile on the Login screen to initiate a deletion sequence.
* Safety Check (`kc_input.js`): Added the `LOGIN_DELETE` state, which prompts the user to confirm the deletion with `Enter` or back out with `Escape` to prevent accidental data loss.
Changelog (v3.3.1 Hotfix)
* Watchdog Directive (`rules.md`): Added UX standards for First-Letter Navigation and Visual State Indicators (CSS class toggling).
* Visual Shift (`style.css`, `kc_hub.js`): The Terminal Login screen now applies a `.theme-login` CSS class to shift the global font and border colors to Amber, providing a high-contrast visual indicator of the system state. The color shift persists through the New Cadet input screen and reverts to standard green upon successful login.
* First-Letter Navigation (`kc_input.js`): Added logic to the `LOGIN` state allowing users to press letter keys to cycle through roster names (e.g., pressing "C" cycles between "Create New Cadet" and "Charlie").
* Login Audio Fix (`kc_hub.js`): Split the `renderLogin` audio logic. Initial boot uses a 600ms delay, but arrow/letter-key navigation now triggers an immediate (0ms) announcement of the highlighted profile.
* Roster Deduplication (`kc_input.js`): Added case-insensitive name checking to the `LOGIN_INPUT` state. Attempting to create a profile that already exists will trigger an error and return the user to the roster menu.
* UX Fallback (`kc_input.js`): Users can now press `Escape` during new profile creation to abort and return to the Login menu.
Changelog (v3.3)
* Multi-User Architecture (`kc_core.js`): Deprecated the single-save `localStorage` key in favor of a Master Roster system (`kbc_roster`) that manages individual save files (`kbc_profile_[Name]`) for multiple students on shared lab computers.
* Terminal Login Screen (`kc_hub.js`): Replaced the immediate Deck 0 boot sequence with a Profile Selection menu. Users can now select an existing Cadet profile or choose "Create New Cadet" to start fresh.
* Login Input Routing (`kc_input.js`): Added keyboard navigation support for the new `LOGIN` and `LOGIN_INPUT` system states, seamlessly catching user names from the input trap and generating new save profiles.
Changelog (v3.2)
* Watchdog Directive (`rules.md`): Added the "Menu Clarity" standard to Section 4 to ensure all future UI additions use plain-English descriptions and noun-based labels.
* UX/Accessibility (Deck Menu): Added plain-English, one-sentence descriptions (`desc` property) to all Hub Menu items in `kc_data.js` to reduce cognitive load for new users. 
* Menu Label Cleanup (`kc_data.js`): Renamed "Enter Arcade" to "Arcade" and "Access Archive" to "Archive" for grammatical consistency across the facility list.
* Audio Formatting (`kc_hub.js`): Updated the menu navigation logic so the screen reader seamlessly stitches the title, description, and reward estimate together into a single, cohesive announcement.
* UI Formatting (`kc_mission_core.js`): Updated `getMissionRewardEstimate` to prefix rewards with "from" (e.g., "Reward from 10 Data Blocks") to clarify that payouts scale with difficulty.
## Changelog (v3.1)
* UI/Audio Cleanup (kc_mission_core.js, mission_race.js): Simplified the report card title strings across all mission types (System Override, Data Dump, and Keyboard Commander) to read simply "MISSION COMPLETE". This eliminates the redundant announcement of the Transmission Rate or Race Score in the title, as those metrics now have dedicated, explicitly labeled lines in the standardized 5-line receipt.
\## v3.0.0 Added to Git

Changelog (v2.96.4)

• 

Race Rewards Synchronization (mission\_race.js): Overhauled the endRace reward calculation to correctly track, award, and display Sync Sparks and Consecutive Coins, matching the 5-line receipt used by System Override.

• 

Failure Screen Synchronization (kc\_mission\_core.js): Updated the triggerMissionFail function to use the standardized 5-line receipt (with zeroed-out values) so failed missions share the exact same layout as successful ones.







Changelog (v2.96.3)

• 

Reward Log Enforcement (kc\_mission\_core.js): Replaced conditional if-statements with a strict 5-line array in the completeLesson function. This guarantees that all currencies (Data Blocks, Logic Shards, Sync Sparks, Consecutive Coins, and Glitch) are always passed to the summary screen and announced by the screen reader, even if the earned value is zero.





Changelog (v2.96.2)

• 

Report Card Enforcement (kc\_mission\_core.js): Removed the conditional "if" statements from the reward logic. The engine now mandates a strict 5-line receipt (Data Blocks, Logic Shards, Sync Sparks, Consecutive Coins, and Glitch) for every mission, ensuring complete parity between Keyboard Commander and System Override.

• 

Audio Summary Verification: Re-wired the audioSum variable to cleanly read out the full reward array so JAWS and NVDA announce all 5 metrics sequentially.

• 

Code Integrity: Replaced the entire functions to eliminate any risk of truncation from previous fast-model outputs.





Changelog (v2.96.1)

• 

Mission Summary Synchronization (kc\_mission\_core.js): Updated the report card to always display all five currency categories (Data Blocks, Logic Shards, Sync Sparks, Consecutive Coins, and Glitch). This ensures the visual and audio "receipt" is identical across all mission types, even if the reward for a specific category is zero.

• 

Audio Summary Expansion: Updated the screen reader announcement to list every currency result immediately, providing full transparency of the mission's impact on your wallet.

• 

False Perfect Failsafe: Standardized the rewardLog formatting to ensure that mission failure states properly display zeroed-out rewards rather than leaving categories blank.





Changelog (v2.96)

• 

Mission Fail Logic Fix (kc\_mission\_core.js): Fixed a parameter mismatch in triggerMissionFail that was causing failed missions to display successful stats.

• 

Report Card Parameter Sync: Re-aligned the showReportCard arguments to ensure Accuracy, Transmission Rate, and Max Streak are correctly mapped.

• 

Catastrophic Failure Branding: Standardized the failure messages to "MISSION CRITICAL FAILURE" for better screen reader feedback.





Changelog (v2.95.1)

• 

Report Card UI Update (kc\_mission\_core.js): Replaced the "Speed: WPM" label with "Transmission Rate (TRS)" to accurately reflect the new speed metric.

• 

Audio Announcement Sync: Updated the screen reader announcement to prioritize Transmission Rate over WPM during the mission summary.





Changelog (v2.95)

• 

Data Cache Expansion (kc\_hub.js): Updated the Data Cache terminal to include the two new currencies: Sync Sparks and Consecutive Coins.

• 

Integrated Acquisition Intel: Each currency line in the Data Cache now includes a concise explanation of how it is earned, optimized for screen reader navigation.

• 

Tabular Persistence: Maintained the two-tab system (Wallet vs. Inventory) to ensure a familiar and organized user experience.





Changelog (v2.94)

• 

Resource Expansion \& Screen Reader Optimization (kc\_data.js): Added "Sync Sparks" and "Consecutive Coins" to the registry and renamed "Glitch Mass" to "Glitch" for better accessibility.

• 

Performance-Based Economy (kc\_mission\_core.js):

• 

Transmission Rate (TRS): Introduced a new speed metric (Keystrokes Per Second) that ignores "audio bottlenecks."

• 

Dynamic Rewards: Data Blocks scale with keystrokes; Logic Shards require 90% accuracy; Sync Sparks scale by TRS x Difficulty; Consecutive Coins scale by 15-key streaks.

• 

Glitch Penalties: Applied 1:1 on missions and a +5 catastrophic penalty on failure.

• 

Dynamic Reporting: The summary screen now generates a detailed receipt of all earned resources.





Changelog (v2.93)

• 

Hub Menu Restoration (kc\_data.js): Restored the missing buttons for System Fabricator, Echo Chamber, and Save and Exit to the Deck 0 Hub menu without reverting the recent Decoupled Menu structural upgrades.





Changelog (v2.92)

• 

Stream Menu Routing Fix (kc\_mission\_core.js): Updated the getRegions function so that Data Dump (generator === "stream") uses the regionsReflex menu map instead of regionsRace. This correctly removes the Expert zones and the forced-vowel zone from the Data Dump mission briefing.





Changelog (v2.92)

• 

Stream Menu Routing Fix (kc\_mission\_core.js): Updated the getRegions function so that Data Dump (generator === "stream") uses the regionsReflex menu map instead of regionsRace. This correctly removes the Expert zones and the forced-vowel zone from the Data Dump mission briefing.





Changelog (v2.91)

• 

Menu Propagation Fix (mission\_race.js, mission\_stream.js): Updated the sequence generators to dynamically request the correct regions map via KC.mission.getRegions(lesson). This prevents the "undefined regions" crash that was silently stalling the games immediately after the launch countdown.





Changelog (v2.90)

• 

Input Routing Fix (kc\_mission\_core.js): Renamed the hand-selection arrow key handler back to changeMissionSetting to perfectly match the internal calls coming from kc\_input.js. Left and Right arrows will now resume cycling the Left, Right, and Both Hands modes.





Changelog (v2.89)

• 

Reflex Label Correction (kc\_mission\_core.js): Removed the inaccurate "(+ Vowels)" tag from the third target zone in regionsReflex.

• 

Data Verification: Confirmed the character strings for the Home \& Bottom Row zone are strictly limited to those two rows, ensuring no top-row characters leak into the System Override generator.





Changelog (v2.88)

• 

Decoupled Mission Menus (kc\_mission\_core.js): Split the single regions array into regionsRace and regionsReflex to create dedicated, isolated target maps for Keyboard Commander and System Override.

• 

State Carryover Fix: Added bounds-checking to getActiveKeys, renderMissionStart, and changeMissionRegion so the game safely catches out-of-bounds Expert indexes when swapping between different mini-game menus.





Changelog (v2.87.1)

• 

Reflex Initialization Fix (mission\_reflex.js): Removed legacy countdown logic from the start function to sync with the new centralized countdown in the core engine. The mission will now immediately transition to beginTyping() when the core countdown finishes.

• 

TTS "Undefined" Fix (mission\_reflex.js): Added fallback strings for dynamically generated missions that lack hardcoded briefing text.





Changelog (v2.87)

• 

Launch Crash Fix (kc\_mission\_core.js): Fixed a critical bug in executeMission where the engine was attempting to announce undefined variables during the launch sequence.

• 

SC Routing Stabilization: Added a safety check to ensure the System Control (Reflex) handler is properly initialized before the countdown starts.





Changelog (v2.86.2)

• 

Audio Bank Expansion (kc\_data.js): Added Snippy's dedicated number files (0 through 10) and the word "Go" to the GAME\_DATA.audio\_bank using the \_sn naming convention.

• 

Countdown Audio Fix (kc\_mission\_core.js): Rewired the runCountdown function to call Snippy's specific audio keys (num\_sn\_3, num\_sn\_2, num\_sn\_1, word\_go\_sn) instead of falling back to Amelia's character keys or the screen reader.





Changelog (v2.86.1)

• 

Audio Collision Fix (mission\_race.js): Stripped the redundant countdown logic out of the Race engine's initialization. The game now relies strictly on the core engine's countdown, freeing up the audio channel so Snippy's voice files can play cleanly.

• 

TTS String Cleanup (kc\_mission\_core.js): Removed the redundant "Target Zone:" prefixes from the regions array names to prevent the screen reader from echoing the phrase twice during menu navigation.





Changelog (v2.86)

• 

Audio Overlap Fix (kc\_mission\_core.js): Removed redundant text-to-speech announcement calls during the countdown sequence. This prevents the audio system from echoing and talking over the actual voice files.

• 

TTS String Cleanup: Changed the fallback text in the countdown from "3..." to "Three" to prevent the system from awkwardly reading punctuation out loud.





Changelog (v2.85)

• 

Universal Voice Actor Routing (mission\_race.js): Implemented an audioMap system to correctly route audio keys.

• 

Strict Junie Enforcement (mission\_race.js): The Race engine will now actively filter out any words from the pool that do not have a Junie (\_ju) audio file attached, universally applying this rule to all Target Zones within the mission.





Changelog (v2.84)

• 

No-Repeat Logic (mission\_race.js): Replaced the purely random word selection with a "shuffle bag" algorithm. Words are now pulled and removed from a temporary array, guaranteeing every available word is used before any repeats occur.

• 

Dynamic Race Length (mission\_race.js): Fixed a hardcoded limitation that forced all races to be exactly 20 words long. The race will now correctly respect the Short/Medium/Long settings chosen in the Mission Briefing.





Changelog (v2.83)

• 

Input Routing Restored (kc\_mission\_core.js): Fixed a critical oversight in executeMission where the activeHandler and ACTIVE\_TYPING state were not being set. All keystrokes, including Spacebar and Enter, will now properly route into the active mission engine.





Changelog (v2.82.3)

• 

Silent Crash Fix (kc\_mission\_core.js): Removed nonexistent UI calls (KC.ui.showView) from loadLesson and correctly routed it to this.renderMissionStart(lesson).

• 

Launch Execution Fix: Renamed startMission to executeMission to properly align with kc\_input.js's keybindings.

• 

Data Dump Support: Added the stream generator routing into executeMission so the Data Dump mission will launch correctly.





Changelog (v2.82.2)

• 

Menu Routing Fix (kc\_hub.js): Restored the missing activateMenuItem function. Pressing Enter on a mission in the Hub menu will now correctly route the target\_id to the Mission Briefing screen.





Changelog (v2.82.1)

• 

Syntax Fix (mission\_race.js): Removed duplicate variable declarations in the start function to prevent fatal Javascript errors.





Changelog (v2.82)

• 

Hub Menu Cleanup: Removed standalone Bottom Row drills. Consolidated all typing zones into the Keyboard Commander / System Override internal briefing menus.

• 

Data Structure Fix: Converted GAME\_DATA.lessons to an Object structure to ensure perfect string-ID lookups.

• 

Race Engine Upgrade: Rewired mission\_race.js to use KC.mission.isWordInZone(), allowing Keyboard Commander to properly serve words with vowels in the restricted target zones.

• 

Target Zone Finalization: Locked in the 5 core Target Zones (Home, Top Standard, Top Expert, Bottom Standard, Bottom Expert) into the core engine.





Changelog (v2.81)

• 

Word Filter Fix (kc\_mission\_core.js): Updated the isWordInZone function to act as "vowel-aware". It will no longer reject a word simply because it contains a vowel that falls outside the specific target row's keys.





Changelog (v2.80.3)

• 

Data Structure Alignment (kc\_data.js): Converted the lessons array into a keyed object. This allows the engine to instantly find a mission by its ID (System Override, Keyboard Commander, or the new Bottom Row tiers) without searching through a list.

• 

Bridge Compatibility (kc\_mission\_core.js): Synchronized the lookup logic to ensure the activeLesson is properly populated from the new object structure.





Changelog (v2.80.2)

• 

Core Bridge Fix (kc\_mission\_core.js): Completely refactored loadLesson and startMission to correctly handle string IDs from the Hub menu.

• 

State Initialization: Ensured that activeLesson is properly populated before the briefing screen renders.

• 

Mission Linkage: Fixed the logic in startMission so it properly triggers the reflex (SC) or race (KC) handlers based on the mission data.





Changelog (v2.80.1)

• 

Core Bridge Fix (kc\_mission\_core.js): Rewrote loadLesson to ensure the lesson data is explicitly locked into KC.state.activeLesson before transitioning to the briefing.

• 

Launch Activation: Updated startMission to be more aggressive in finding the lesson data, with a fallback mechanism to prevent the "silent fail" you are seeing.

• 

Input Routing (kc\_input.js): Verified that the "Enter" key on the briefing screen correctly calls the fixed startMission function.





Changelog (v2.80)

• 

Core Launch Logic (kc\_mission\_core.js): Updated the startMission function to dynamically support the new 5-region architecture.

• 

Generator Bridge: Fixed the routing between the Mission Briefing screen and the reflex / race handlers so that the new Bottom Row indexes (3 and 4) correctly trigger word selection.

• 

Input Routing (kc\_input.js): Ensured that the "Enter" key on the briefing screen correctly passes the expanded region data to the active handler.





Changelog (v2.79)

• 

Mission Data Sync (kc\_data.js): Updated the regionMode indexes for the Bottom \& Home Row missions to match the new 5-zone menu structure.

• 

Logic Calibration:

• 

Standard Bottom Row: Now correctly points to regionMode: 3 (Home \& Bottom Row + Vowels).

• 

Expert Bottom Row: Now correctly points to regionMode: 4 (Bottom \& Home Row Expert with Vowels).

• 

Expert Word Pool Activation: Confirmed that the Expert difficulty (difficulty: 5) is correctly mapped to trigger the longer word dictionaries for both Top and Bottom row sessions.





Changelog (v2.78.1)

• 

UI Label Refinement (kc\_mission\_core.js): Reorganized the regions array to follow the requested progression: Home Row, Top/Home (Standard), Top/Home (Expert), Bottom/Home (+ Vowels), and Bottom/Home Expert.

• 

Consistency Fix: Removed the "Standard" suffix from the Top/Home entry and the redundant fourth region to keep the menu streamlined.

• 

Vowel Indicator: Added "with all vowels" to the Bottom Row Expert label to clearly communicate the character set to the user.





Changelog (v2.78)

• 

Mission Menu Cleanup (kc\_data.js): Consolidated the Bottom Row missions into two specific targets: Standard and Expert.

• 

UI Label Refinement: Updated the labels to explicitly state "(+ Vowels)" for both Bottom Row tiers.

• 

Expert Mission Activation: Formally mapped the Expert word pools for both Top and Bottom row missions.





Changelog (v2.77)

• 

Mission Logic Fix (mission\_reflex.js \& kc\_mission\_core.js): Updated the isWordInZone filter in both the core engine and the reflex handler to globally allow all vowels (aeiou). This ensures that Bottom Row missions correctly include the full word bank instead of being restricted to only words containing "A".

• 

UI Label Update (kc\_mission\_core.js): Renamed the "Home \& Bottom Row" zone to "Home \& Bottom Row (+ Vowels)" so the mission setup screen explicitly reflects the expanded character set.

• 

"No-Repeat" Implementation: Fully integrated the sessionPool logic into both mission\_race.js and mission\_reflex.js. The engine now cycles through every available word in the pool before any word is repeated.

• 

Version Sync: Updated all core files to v2.77.





Changelog (v2.76)

• 

Mission Manifest Expansion (kc\_data.js): Added a full suite of "Bottom \& Home Row" missions to the Hub menu.

• 

New Mission Tiers: Integrated Left Hand, Right Hand, and Both Hands challenges for both Standard and Expert difficulty levels.

• 

Audio Routing: Configured these missions to specifically use the \_ju (Junie) audio prefix.

• 

Logic Sync: Set these missions to target the home and bottom row zones.





Changelog (v2.75)

• 

Mission Logic (mission\_race.js): Implemented a "No-Repeat" word selection system.

• 

Word Pool Management: Added sessionPool and usedInSession tracking to KC.handlers.reflex and KC.handlers.race. The engine now clones the master word list at the start of a mission and removes words as they are used. The pool only resets (refills) once it is completely empty.

• 

Version Sync: Updated kc\_data.js and kc\_mission\_core.js to 2.75.





Changelog (v2.74)

• 

Audio Bank Expansion (kc\_data.js): Injected over 100 new word-to-audio mappings for the Bottom \& Home Row set, including Standard and Expert tiers for Left, Right, and Both hands.

• 

Mission Logic Sync (mission\_race.js): Updated the internal versioning to 2.74.

• 

Core Engine Update (kc\_mission\_core.js): Bumped the core version to 2.74 to support the new data structures.





Changelog (v2.73.1)

• 

Audio Bank Expansion (kc\_audio.js): Added synthesizer profiles for sounds 30 (Machine Rev), 34 (Comm Link), 57 (Spark), and 62 (Springy Boing).

• 

Mission UI Audio Wiring (kc\_mission\_core.js): Wired up dynamic synth feedback for the mission setup screen:

• 

Sound 62 (Springy Boing): Triggers when adjusting target hands (Left/Right).

• 

Sound 34 (Comm Link): Triggers when shifting target zones (Up/Down).

• 

Sound 57 (Spark): Triggers when setting difficulty (1-5).

• 

Hub Menu Audio Wiring (kc\_input.js): Routed the Data Cache and Archive menu selections to trigger Sound 30 (Machine Rev) instead of the standard menu beep.





Changelog (v2.73)

• 

Web Audio API Synth (kc\_audio.js): Integrated a standalone playSynth function into the audio manager, allowing the game to dynamically generate math-based audio frequencies without needing .mp3 files.

• 

Audio UI Wiring (kc\_input.js \& kc\_mission\_core.js): Wired up 6 custom synthesized tones:

• 

Sound 3 (Floor Up): Mapped to adjusting mission length up (Page Up).

• 

Sound 4 (Floor Down): Mapped to adjusting mission length down (Page Down).

• 

Sound 17 (Power Up): Mapped to launching a mission (Enter).

• 

Sound 18 (Power Down): Mapped to globally aborting missions or exiting menus (Escape).

• 

Sound 23 (Menu Select): Mapped to standard Hub menu selection (Enter / Right Arrow).

• 

Sound 24 (Lifeboat / Echo): Mapped to launching the Echo Chamber from the Hub and the Echo Chamber start screen.





Changelog (v2.72)

• 

UI Audio Feedback (kc\_mission\_core.js): Injected the standard menu navigation sound (KC.audio.playSound('click')) into all mission parameter adjustment functions (changeMissionSetting, changeMissionRegion, changeMissionLength, changeDifficulty). Players will now hear an instant "bloop" when toggling settings on the Briefing screen, right before the TTS announces the change.





Changelog (v2.71)

• 

Audio Bank Mapping (kc\_data.js): Added 7 missing dictionary mappings for CK's mechanical keyboard clicks (sfx\_key\_1 through sfx\_key\_7), pointing them to the /audio/sfx/ folder. The race engine will now successfully trigger the physical typing sounds.

• 

Version Sync (mission\_race.js): Bumped version header to 2.71 to maintain continuity.





Changelog (v2.70.1)

• 

Mission Abort Patch (mission\_race.js): Updated the endRace function to actively receive the aborted flag. It now successfully clears all of Amelia's and CK's ghost audio timers (spellTimers and ckTimer) and safely calculates a partial report card without granting victory rewards.





Changelog (v2.70)

• 

Audio Leak Prevention (mission\_race.js): runCKBehavior and spellWord now strictly check that the game state is still ACTIVE\_TYPING before playing audio. Added a spellTimers array to track and instantly kill Amelia's spelling sequence if the mission is aborted.

• 

Mission Abort Logic (mission\_race.js): Overhauled endRace to accept an aborted flag. If a player aborts, the mission immediately halts, calculates current stats, skips rewards, and drops them at the Mission Report screen.

• 

Global Intercept (kc\_mission\_core.js \& kc\_input.js): Added abortActiveMission() to the core engine. Rerouted the global Escape key so that pressing it during an active mission securely triggers this abort sequence instead of dumping the player straight into the Hub.





Changelog (v2.69.1)

• 

Strict Zone Enforcement (mission\_race.js): Removed the sequence generator failsafe. The engine now strictly enforces the allowedChars array. If a target zone lacks compatible words, the mission safely aborts rather than falling back to the global dictionary.

• 

Length Exemption (mission\_race.js): Exempted the "Home Row" target zone from standard/expert length constraints, allowing any compatible word to populate the sequence.

• 

Dynamic Audio Router (mission\_race.js): startRound now dynamically checks the audio\_bank for a Junie (word\_ju\_) prefix first, and cleanly falls back to a Snippy (word\_) prefix if needed.

• 

Home Row Audio Bank (kc\_data.js): Injected 24 pure Home Row word mappings (word\_ju\_) into the audio\_bank so the Keyboard Commander race logic can successfully generate Home Row races without aborting.





Changelog (v2.69)

• 

Target Zone Refinement (kc\_mission\_core.js): Removed the isolated "Top Row" and "Bottom Row" target zones. Replaced the legacy "Home \& Bottom Row" with two new split zones: "Bottom \& Home Row (Standard)" and "Bottom \& Home Row (Expert)" featuring Left, Right, and Both hand quadrants.

• 

Audio Layering (mission\_race.js): Increased the delay on CK's mechanical keyboard click SFX from 50ms to 120ms so it doesn't get masked by his voice files.

• 

Amelia Spelling Assist (mission\_race.js): Added a dynamic spellWord function. Amelia now sequentially spells out the target word parallel to CK's typing to assist players in matching the correct spelling.





Changelog (v2.68.1)

• 

New Racing Target Zones (kc\_mission\_core.js): Replaced the legacy "Home \& Top Row" with two new distinct target zones: "Top \& Home Row (Standard)" and "Top \& Home Row (Expert)". These fully support Left, Right, and Both hand quadrants.

• 

Dynamic Race Generation (mission\_race.js): Overhauled the sequence builder. The Commander now pulls the exact active keys from your selected target zone, and intelligently filters the Junie \_ju audio words by length (<= 5 characters for Standard, >= 6 characters for Expert).





Changelog (v2.68)

• 

Junie Audio Bank Expansion: Injected 99 new Junie (\_ju.mp3) voice assets into the kc\_data.js audio bank. These are mapped with the word\_ju\_ prefix, directly preparing the engine for the dynamic race generation logic in the Keyboard Commander mode.





Changelog (v2.67)

• 

New Target Zone: Added "Home \& Bottom Row" to the centralized target zones in kc\_mission\_core.js.

• 

Target Zone Lock: Applied the forceBoth: true flag to the new "Home \& Bottom Row" zone, locking out the left/right hand selection menu and requiring the use of all keys across both hands.





Changelog (v2.66.2)

• 

Audio Bank Restoration: Re-mapped 35 Snippy audio assets (\_sn.mp3) for the Home Row target zone into the kc\_data.js audio bank. Systems Control will now successfully play these assets instead of falling back to TTS.





Changelog (v2.66.1)

• 

Audio Bank Restoration (kc\_data.js): Mapped the legacy tutorial audio files for Fuel, Drive, Stabilizers, and Auxiliary to their dynamic word\_ aliases in the audio\_bank. The Systems Control generator will now successfully trigger Snippy's original voice lines for these targets instead of failing silently.





Changelog (v2.66)

• 

Dynamic Reflex Generation: Completely overhauled generateReflexSequence inside mission\_reflex.js. The Systems Control game mode now dynamically polls KC.mission.getActiveKeys() to support all row and region selections (Top Row, Bottom Row, Symbols, etc.).

• 

System Words Integration: Systems Control now intelligently cross-references the randomly selected target key with the GAME\_DATA.system\_words dictionary, generating a dynamic and highly randomized audio experience using Snippy's voice assets.

• 

Symbol/Modifier Fallback: If a key does not have a designated system word (like Numbers or Arrow Keys), the engine intelligently falls back to the exact char\_ audio asset and formats the TTS text to match.





Changelog (v2.65.1)

• 

Syntax Repair (kc\_mission\_core.js): Fixed a critical typo (BKC.handlers) at the top of the file that was crashing the mission module and breaking the Hub menu's mission links.

• 

Accessibility Restoration (kc\_hub.js \& kc\_input.js): Re-applied the screen reader focus fixes that were lost during the version rollback. The Hub menu once again utilizes setTimeout to decouple DOM updates from TTS announcements, and the input router applies blanket e.preventDefault() suppression to eliminate cursor bleed and "Value Thrashing" during menu navigation.





Changelog (v2.65)

• 

System Words Dictionary: Re-integrated the system\_words lookup dictionary into kc\_data.js. This creates the data foundation for Systems Control to randomize dynamic sci-fi audio cues.

• 

Expanded Target Zones: Re-integrated "Home \& Top Row", "Symbols", and "Arrows \& 6-Pack" into the regions array in kc\_mission\_core.js, complete with the correct forceBoth: true lockouts to prevent invalid hand-selection.



Changelog (v2.6.0)

• 

Audio Library Expansion: Ingested 66 new audio assets into the engine's audio\_bank, encompassing the Function Keys (F1-F12), the Navigation Cluster (Arrows, Home, End, PageUp, PageDown, Insert, Delete), and Shifted Number Row Symbols (!, @, #, etc.).

• 

Dynamic Audio Aliasing: Created direct aliases mapping the browser's native e.key events (like arrowup, pagedown, f1, and \&) to their respective \_ame.mp3 files, making them instantly playable in the Echo Chamber.







Changelog (v2.5.3)

• 

New Target Zone: Added "Entire Alphabet" to the KC.mission.regions array, which encompasses all 26 standard letters.

• 

Hand-Selection Lockout: The "Entire Alphabet" zone features a forceBoth constraint. When active, Left/Right arrow keys for hand selection are locked out and will inform the user: "Entire Alphabet utilizes all keys".

• 

Dynamic Briefings: Updated the Mission Briefing UI and Text-to-Speech announcements in both kc\_mission\_core.js and mission\_stream.js to elegantly omit the "Left/Right/Both Hands" suffix when a region forces both hands.





Changelog (v2.5.2.4)

• 

Silent Modifiers (Input Handling): Added an interception layer at the absolute top of the global kc\_input.js router. Standalone Shift, Control, Alt, and Meta (Windows/Command) keys are now ignored by the event listener.

• 

Echo Chamber UX: Pressing chorded keys (like Shift + \[) will now cleanly and exclusively announce the resulting character (e.g., "Open Brace") without preceding it with the modifier's name.





Changelog (v2.5.2.3)

• 

Audio Engine Hotfix: Corrected an encoding error where the "less than" (<) and "greater than" (>) symbol aliases were incorrectly formatted as HTML entities. These physical keystrokes will now properly map to their audio files and verbalize in the Echo Chamber.





Changelog (v2.5.2.1)

• 

Audio Engine Wiring: Added 29 new dynamic aliases to the audio\_bank mapping browser keystroke events (like "shift", "escape", "\~", and " ") directly to the newly imported modifier and symbol audio files.

• 

Echo Chamber Compatibility: Modifiers and Symbols will now instantly verbalize with Amelia's voice when pressed in the Echo Chamber.





Changelog (v2.5.2)

• 

Audio Library Expansion: Ingested 74 new audio assets into the engine's audio\_bank, covering Amelia's voice lines for the Number Row, Modifiers, and Symbols.

• 

Dynamic Audio Aliasing: Created direct aliases for the Number Row (char\_1 through char\_0) and Bottom Row punctuation (char\_, and char\_.). This allows the Data Dump and System Override mini-games to instantly speak these characters when their respective target zones are selected, requiring no changes to the game logic.

&#x20;Changelog (v2.5.1)

• 

New Target Zone: Added "Entire Alphabet" to the KC.mission.regions array, which encompasses all 26 standard letters from the top, home, and bottom rows.

• 

Hand-Selection Lockout: The "Entire Alphabet" zone features a new forceBoth constraint. When this zone is selected, Left/Right arrow keys for hand selection are locked out and will inform the user: "Entire Alphabet utilizes all keys".

• 

Dynamic Briefings: Updated the Mission Briefing UI and Text-to-Speech announcements in both kc\_mission\_core.js and mission\_stream.js to correctly omit the "Left/Right/Both Hands" suffix when the Entire Alphabet zone is active.





Changelog (v2.5.0)

• 

Core Systems: Centralized keyboard region definitions into KC.mission.regions to support cross-deck scaling.

• 

Mission Configuration: Added regionMode to the mission parameters. Players can now use the Up and Down arrow keys on the mission start screen to toggle between Home Row, Top Row, Bottom Row, and Number Row.

• 

Data Dump: Updated mission\_stream.js to dynamically generate threats based on the player's selected row and hand quadrant.

• 

Input Routing: Mapped ArrowUp and ArrowDown inside the MISSION\_START state to cycle the newly defined keyboard rows.





Changelog (v2.4.0)

• 

Feature Restoration: Re-enabled the "Data Cache" in the Deck 0 Main Hub menu.

• 

Data Cache Logic: Fully fleshed out the renderDataCache and MapsCacheContent functions in kc\_hub.js.

• 

Inventory Management: The Data Cache now features two tabs: "Wallet" (displays currencies like Data Blocks, Logic Shards, and Glitch) and "Inventory" (displays owned items like Patches and Modules). It fully supports accessible Up/Down vertical navigation.





Changelog (v2.3.1)

• 

Archive Bug Fix: Fixed a silent crash (TypeError) caused by the Archive state missing from initialization. Navigating left and right now correctly cycles the tabs and announces them.

• 

Audio Feedback: Pressing Enter on any item in the Main Hub Menu now triggers a standard click sound to confirm the selection before loading the new environment.

• 

Archive Context: Entering the Archive now explicitly prefixes the audio announcement with "Archive Accessed." for better locational awareness.





Changelog (v2.3.0)

• 

Screen Reader UX (Grid Navigation): Standardized 2D menu navigation across all systems (Fabricator, Engineering, Arcade). Navigating Left/Right or entering a menu announces the Category Name followed by the Item Name. Navigating Up/Down within a category now strictly announces only the Item Name to prevent redundant chatter.

• 

Hub Audio Experience: Returning to the Hub now parses the current deck's nickname (e.g., "Garbage Deck") and announces it prior to reading the focused menu item, providing immediate locational awareness without repeating it during vertical menu navigation.

• 

Archive Navigation: Clarified the reading logic so switching tabs announces the Tab name, while scrolling vertically reads only the specific log entries.





Changelog (v2.2.0)

• 

Hub Navigation: enterHub() no longer resets your cursor position to the top of the list when returning from a mission or sub-menu. It remembers exactly where you left off.

• 

Hub Audio: Removed the redundant "Connected to Deck 0" announcement upon returning to the Hub, ensuring the screen reader immediately reads the currently focused menu item.

• 

Fabricator Bug Fix: Fixed a variable ReferenceError that was causing the Text-to-Speech engine to crash silently while navigating the Fabricator.

• 

Audio Feedback: Added missing interface click sounds when navigating the Fabricator, Engineering, Arcade, and Archive menus with the arrow keys.





Changelog (v2.1.1)

• 

Data Dump (Rebalancing): Adjusted the difficulty speed curve to be much more forgiving. Novice now drops data at 1600ms, scaling down to Commander at 800ms.

• 

Data Dump (Phase Lengths): Reduced the phase duration configurations. Short is now 10s per phase (30s total), Medium is 20s per phase (60s total), and Long is 30s per phase (90s total).

• 

UI Updates: Updated the mission briefing display and screen reader announcements in kc\_mission\_core.js to accurately reflect the new timing and difficulty parameters.





Changelog (v2.1.0)

• 

Data Dump (Phasing \& Difficulty): Base speed is now directly tied to the Difficulty setting chosen at the start screen (Novice = 800ms, Commander = 400ms).

• 

Data Dump (Dynamic Scaling): Total time is now split into 3 equal phases (Length mode determines if each phase is 20s, 30s, or 40s). The drop speed in Phases 2 and 3 dynamically calibrates based on the player's average reaction time in the previous phase (it can speed up or slow down to match their exact skill level).

• 

Data Dump (Threat Audio): Maximum buffer capacity drastically reduced to 6. The audio cue for an incoming data block now escalates based on how full the buffer is:

• 

Queue 1-3: Liquid "Bloop" sound.

• 

Queue 4: Square wave "Warning Thunk" + "Warning" voice cue.

• 

Queue 5: Harsh sawtooth "Critical Buzz" + "Critical" voice cue.

• 

Queue 6: Buffer Overflow (Game Over).

• 

UI/Core: Updated kc\_mission\_core.js to accurately display the per-phase duration values in the Hub briefing.





Changelog (v2.0.2)

• 

Bug Fix: Added mission\_stream.js to index.html so the Data Dump game mode can properly load.

• 

System Cleanup: Permanently removed the legacy "Audio Diagnostics" (Audio Lab) option from the main menu, lesson definitions, and core mission routing.





Changelog (v2.0.1)

• 

Data Dump (Bug Fix): Fixed an issue where the score earned during the mission was not being converted into Data Blocks upon victory.

• 

Data Dump (Accessibility): Updated the fallback Text-to-Speech announcer to cast characters to uppercase before speaking, preventing screen readers from mangling single lowercase letters.





Changelog (v2.0.0)

• 

System Update: Keyboard Commander Engine upgraded to v2.0.0.

• 

Data Dump Overhaul: Completely rewrote the Data Dump mini-game logic (mission\_stream.js) into a true time-based FIFO survival mode.

• 

Decoupled Audio UX: Threat generation (the "Drop") is now decoupled from player input, utilizing a mechanical click synth to indicate rising queue pressure without spamming the screen reader. Voice prompts are now strictly reserved for the single active target at the front of the queue.

• 

Dynamic Difficulty: Drop speeds in Phases 2 and 3 are now dynamically calculated based on the player's average reaction time in the preceding phase.

• 

Reaction Scoring System: Added performance tracking that awards tiered score multipliers for lightning-fast reaction times (< 500ms and < 1000ms).

• 

Tactical Readout: Added a panic button. Pressing Spacebar now safely reads out the current queue depth and active target without registering as a typing error.





• 

Reflex Mode: Tracks consecutive correct keystrokes.

• 

Race Mode: Tracks consecutive rounds won against Commander Keyboard.

• 

LOGIC (Grading): Added a grading algorithm that weighs Accuracy heavily, awarding an S Rank only for 100% accuracy.

Updated Files (4)

Changelog v1.73.6

• 

FIX (Static Initiation): Fixed a bug where the static sound loop started at default volume immediately upon mission start. It now correctly initializes at 0 volume (Silence) and only becomes audible after the first mistake.

• 

LOGIC (Streak Recovery): Reduced the requirement to lower static levels from 7 correct inputs to 5 correct inputs.

• 

FIX (Streak Reset): Fixed a logic error where the "Correct Streak" didn't reset when a mistake was made. Now, making an error resets your recovery progress to 0, ensuring you must hit 5 consecutive correct keys to lower the danger level.

• 

VERIFIED: The progression is now: Mistake = Volume Up (+ Streak Reset). 5 Clean Inputs = Volume Down. 4th Tier Mistake = Explosion.

Updated Files (3)



Changelog v1.73.5

• 

AUDIO (Balance): Reduced the volume of the "Instability" static hazard in Systems Control by approximately 50%.

• 

Old Levels: 0, 30%, 60%, 90%.

• 

New Levels: 0, 15%, 30%, 50%.

• 

This should make the static audible but less overwhelming in the early stages of failure.

• 

FEATURE (Helper Cues): Added a "Helpful Snippy" timer to Systems Control (Reflex Mode).

• 

Logic: If you do not type the required key within 2.5 seconds of the command, the system will verbally cue you (e.g., "Type F") to keep the flow moving.

• 

Cancel: This timer is automatically cancelled if you type the correct key before the time is up.

Updated Files (3)



Changelog v1.73.4

• 

FEATURE (Universal Countdown): Upgraded KC.mission.runCountdown in kc\_mission\_core.js. Now, all missions (including Systems Control) utilizing the countdown will play the full "Three... Two... One... GO!" audio sequence with visual updates, ensuring a consistent start across the game.

• 

MECHANIC (System Failure): Implemented the "Reactor Explosion" logic.

• 

In Systems Control (Reflex Mode), making 4 consecutive mistakes (reaching Instability Tier 4) now triggers a catastrophic failure.

• 

Effect: A random explosion sound plays (sfx\_explode\_1 - 7), the screen shakes (simulated via text/audio), and the mission immediately fails.

• 

Consequence: The mission ends in DEFEAT, awarding 1 Glitch Mass and 0 Data Blocks.

• 

LOGIC: Updated mission\_reflex.js to respect the explosion trigger—if the system explodes, the game stops accepting input immediately.



Changelog v1.73.2

• 

MENU (Cleanup): Removed "Code Breaker" from the main Deck 0 Hub menu (Goto Menu) as requested.

• 

UI (Reward Forecasting): Fixed the Data Reward reporting in the Hub Menu. When you highlight a mission (like "Keyboard Commander" or "System Override"), the system now calculates and displays the estimated payout (e.g., "Est. Reward: 50 Data Blocks") alongside the mission name.

• 

AUDIO: The Screen Reader now announces the reward estimate automatically when navigating the menu (e.g., "Keyboard Commander. Mission. Reward 50 Data Blocks.").

• 

LOGIC: Updated kc\_mission\_core.js to return accurate estimates for all active Hub missions.

Updated Files (3)



