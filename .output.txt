Searched codebase for "Please implement the Version 3.14.6 Security & Economy Patch. As per project rules, DO NOT TRUNCATE OR SUMMARIZE CODE. Provide the full, unabbreviated objects/functions being modified.
TASK 1: Update kc_core.js (Expand Currency Action)
Locate the executeAction function inside the KC.dev object. Completely replace the executeAction function with this updated version that provisions a multi-currency wallet:
executeAction: function() {
    switch(this.cursor) {
        case 0: // Close
            this.toggleConsole();
            break;
        case 1: // Warp
            this.toggleConsole();
            KC.core.announce("Warping to Deck 0.");
            if (KC.hub) KC.hub.enterHub();
            break;
        case 2: // Unlock
            if (KC.state.profile) KC.state.profile.currentDeck = 99; 
            KC.core.announce("All missions unlocked.");
            break;
        case 3: // Currencies
            if (KC.state.profile) {
                if (!KC.state.profile.wallet) {
                    KC.state.profile.wallet = { credits: 0, dataBlocks: 0, components: 0 };
                }
                KC.state.profile.wallet.credits += 10000;
                KC.state.profile.wallet.dataBlocks += 10000;
                KC.state.profile.wallet.components += 10000;
            }
            KC.core.announce("Ten thousand units added to all wallet currencies.");
            break;
        case 4: // EXP
            KC.core.announce("EXP Booster applied. Placeholder active.");
            break;
        case 5: // Nuke Save
            KC.core.announce("Save data cleared. Refreshing system.");
            localStorage.removeItem('kc_profile');
            setTimeout(() => location.reload(), 1500);
            break;
    }
},TASK 2: Update kc_core.js (Gate the Dev Console)
Locate the logic inside KC.hub (likely in a render or enterHub function) where the main Hub menu options array is defined. 
Refactor the array generation so that the "Stay Out" option is dynamically added ONLY if the active profile's callsign is "BRIAN" or contains "bot". 
Use logic similar to this:
let options = [ /* standard Hub options... */ ];
let callsign = KC.state.profile && KC.state.profile.callsign ? KC.state.profile.callsign : "";
if (callsign === "BRIAN" || callsign.toLowerCase().includes("bot")) {
    options.push("Stay Out");
}
options.push("Save and Exit");
// Ensure you update whatever properties or loops rely on the menu options array to use this dynamically generated list.TASK 3: Update rules.md
In Section 4 (INPUT & NAVIGATION STANDARDS), update the Developer Operations bullet point. Explicitly state that the Dev Console "Stay Out" menu option is dynamically hidden. It will only render in the Hub if the active profile's callsign is exactly "BRIAN" or contains the word "bot" (case-insensitive).", 22 results. Files: c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.output.txt, c:\nbs\kc\.output.txt, c:\nbs\kc\kc_core.js, c:\nbs\kc\kc_core.js, c:\nbs\kc\kc_core.js, c:\nbs\kc\kc_data.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\rules.md
Thinking: Let me first read the relevant parts of kc_core.js to understand the current KC.hub structure, specifically the render/enterHub function where the menu options are built. I also need to see the current executeAction function to replace it properly.
Let me read the relevant sections first.
Read kc_core.js, lines 100 to 230
Searched for text enterHub (**/kc_hub.js), 4 results. Files: c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_hub.js
Read kc_core.js, lines 298 to 298
Read kc_hub.js, lines 85 to 180
Thinking: The Hub menu is driven by `GAME_DATA.hubs[deckID].menu` (from `kc_data.js`). The `renderGameMenu` and `activateMenuItem` functions in `kc_hub.js` both pull directly from `GAME_DATA.hubs[deckID].menu`. 
For Task 2, I need to make "Stay Out" dynamically added only if the profile's callsign is "BRIAN" or contains "bot". But looking at the profile structure, it uses `name`, not `callsign`. Let me check...
Looking at the `createProfile` function:
Checking the profile structure...
So the profile uses `name` not `callsign`. However, the task says to use `callsign`. Let me check if there's a `callsign` property anywhere. Actually, looking at the task description carefully - it says to use `callsign`, and provides the logic for checking that property.
This might be a name they're introducing or renaming, but let me also check for "callsign" in the codebase.
Let me check whether profiles use
name (c:\nbs\kc\kc_data.js:96)
or callsign, and see the full input handler that handles the hub menu to understand how stay_out is currently dispatched:
Searched for text callsign, 7 results. Files: c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\kc_hub.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\rules.md, c:\nbs\kc\rules.md
Read kc_input.js, lines 285 to 335
Thinking: Now I understand the full picture. The profile uses `name` (not `callsign`), but the task spec uses `callsign`. Since `callsign` is the informal term used throughout the codebase for the player's name (see "Enter your Callsign (Name)" in the UI), I should implement this using `KC.state.profile.name` since that's what's actually stored.
Wait, but the task specification explicitly says to use `KC.state.profile.callsign`. The changelog also refers to "callsign". But based on the code, the profile property is `name`. 
Looking at the task again - it says: "Use logic similar to this:" - so I have flexibility in implementing it. Since the profile uses `.name`, not `.callsign`, I should use `.name` in the implementation to make it work correctly, while following the spirit of the task.
For Task 2, the Hub menu is currently hardcoded in `GAME_DATA.hubs[deckID].menu` in `kc_data.js`, and both `renderGameMenu` and the input handler reference it directly. I need to make "Stay Out" dynamically added instead of static, which means either removing it from the static definition and computing the menu array on the fly in both the render and input handler functions, or taking a different approach to inject it conditionally.
The task description suggests refactoring the array generation logic inside `kc_hub.js`, but the menu actually comes from the data file rather than being built there. The most practical solution is to create a helper function that generates the dynamic menu list by filtering the static menu, then update both `renderGameMenu` and the input handler to use that helper instead of accessing the raw menu directly. Looking at the static menu structure, I see "Stay Out" is hardcoded in there along with all the other options. So I need to create a `getHubMenu()` method on `KC.hub` that pulls the base menu from `GAME_DATA.hubs[deckID].menu`, filters out the "Stay Out" entry by its type, and then conditionally adds it back only if the profile meets the eligibility criteria—this way both the renderer and input handler can call the same method and stay in sync. I've mapped out the three main tasks: replacing `executeAction` in the core file, adding a `getHubMenu()` method to handle the menu filtering logic with the profile name check, and updating both the render function and input handler to use this new method instead of accessing the menu directly. I'm also noting that the data model uses `.name` rather than `.callsign`, so I'll adapt accordingly. Now let me pull up the current `executeAction` code to see what needs to be replaced.
Now let me read the current
executeAction (c:\nbs\kc\kc_core.js:225)
and the exact
rules.md (/c:/nbs/kc/rules.md)
line for replacement targets:
Read kc_core.js, lines 298 to 298
Read rules.md, lines 84 to 92
Searched for text executeAction: function() (**/kc_core.js), 1 result. Files: c:\nbs\kc\kc_core.js
Read kc_core.js, lines 220 to 265
Thinking: Now I have all the context I need. Let me also look at the current `renderGameMenu` function more carefully to understand how to add `getHubMenu()`.
The current flow is:
1. `renderGameMenu` reads `GAME_DATA.hubs[deckID].menu` directly
2. The MENU_NAV input handler reads `GAME_DATA.hubs[deckID].menu` directly
My plan:
1. Add a `getHubMenu(deckID)` method to `KC.hub` that filters "Stay Out" dynamically based on profile name
2. Update `renderGameMenu` to use `this.getHubMenu(deckID)` 
3. Update MENU_NAV input handler to use `KC.hub.getHubMenu(deckID)`
For Task 1, I need to replace the `executeAction` function with the new wallet-provisioning version. For Task 2, let me check the exact implementation in `kc_hub.js` around `renderGameMenu` to see what needs to be modified.
I can see the function retrieves the deck ID and pulls menu items from the game data. I need to extract this menu-fetching logic into a separate `getHubMenu` method, then update both `renderGameMenu` in `kc_hub.js` and the MENU_NAV handler in `kc_input.js` to call it. I also noticed the profile stores the name in `.name` rather than `.callsign`, so I'll construct the callsign variable accordingly. Now I need to find where in `kc_hub.js` to insert the new `getHubMenu` method before `renderGameMenu`.
Now I have everything I need. Let me read just before
renderGameMenu (c:\nbs\kc\kc_hub.js:107)
in
kc_hub.js (/c:/nbs/kc/kc_hub.js)
to get the insertion context:
Read kc_hub.js, lines 93 to 120
All context gathered. Now applying all three tasks simultaneously:
Replacing 41 lines with 52 lines in kc_core.js, kc_hub.js, kc_input.js, rules.md
```
```
```
```
```
```
```
```
```
```
All four files updated. Here's the breakdown:
**
kc_core.js (/c:/nbs/kc/kc_core.js)
—
executeAction (c:\nbs\kc\kc_core.js:225)
case 3:**
Replaced the single currency property increment with a full wallet provisioning block that guards against a missing
wallet (c:\nbs\kc\kc_core.js:97)
object and adds 10,000 to
credits (c:\nbs\kc\kc_core.js:242)
,
dataBlocks (c:\nbs\kc\kc_core.js:242)
, and
components (c:\nbs\kc\kc_core.js:242)
. Announcement updated to reflect multi-currency.
**
kc_hub.js (/c:/nbs/kc/kc_hub.js)
— new
getHubMenu() (c:\nbs\kc\kc_hub.js:107)
+
renderGameMenu (c:\nbs\kc\kc_hub.js:114)
update:**
Added
getHubMenu(deckID) (c:\nbs\kc\kc_hub.js:115)
directly before
renderGameMenu (c:\nbs\kc\kc_hub.js:114)
. It filters
GAME_DATA.hubs[deckID].menu (c:\nbs\kc\kc_data.js:98)
, stripping any stay_out-typed item unless
KC.state.profile.name (c:\nbs\kc\kc_core.js:92)
is exactly &quot;BRIAN&quot; or contains &quot;bot&quot; (case-insensitive).
renderGameMenu (c:\nbs\kc\kc_hub.js:114)
now calls
this.getHubMenu(deckID) (c:\nbs\kc\kc_hub.js:115)
instead of reading the static array directly.
**
kc_input.js (/c:/nbs/kc/kc_input.js)
— MENU_NAV handler:**
const menuItems (c:\nbs\kc\kc_hub.js:116)
now calls
KC.hub.getHubMenu(deckID) (c:\nbs\kc\kc_core.js:35)
so navigation indices,
menuMax (c:\nbs\kc\kc_input.js:290)
, and
selected (c:\nbs\kc\kc_hub.js:163)
all stay in sync with the filtered list — preventing a misaligned selection landing on the wrong item for non-dev profiles.
**
rules.md (/c:/nbs/kc/rules.md)
— Developer Console bullet:**
Updated to explicitly document the dynamic visibility gating (exact &quot;BRIAN&quot; match or &quot;bot&quot; substring, case-insensitive).