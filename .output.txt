Task: Watchdog Update Cycle - Version 3.48.1 (BGM Engine Routing).
We added "bluegrass" to the UI in v3.48.0, but the playback engine is failing silently because the playlist isn't registered.

1. Review `kc_bgm.js`. Locate the `KC.bgm.playlists` object.
    * Inject the new bluegrass playlist at the end of the object: 
      `bluegrass: ["mu_bluegrass1", "mu_bluegrass2", "mu_bluegrass3", "mu_bluegrass4", "mu_bluegrass5", "mu_bluegrass6", "mu_bluegrass7", "mu_bluegrass8"]`
2. Locate the `cycleStyle` function within `kc_bgm.js`.
    * Find the `styleNames` mapping object. 
    * Add `bluegrass: "Bluegrass"` to the list so the TTS engine correctly announces the style when cycling via hotkeys.
3. Apply these surgical diffs directly to `kc_bgm.js`.
4. Update `.changelog.md` with a new entry for v3.48.1 noting the BGM Engine Routing fix.

Maintain strict adherence to S8 (Anti-Truncation). Do not rewrite the entire file, just inject the missing data.