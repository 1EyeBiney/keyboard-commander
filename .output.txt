Searched codebase for "Target Version: v3.45.1
File to Update: mission_stream.js
The Objective:
We are evolving the "Data Dump" mission into a 3-round structure. We need to inject a 4000ms "spacer" (pause) before each phase begins. During this gap, the game must pause drops, clear the buffer, play specific audio cues, and then resume.
The Action Plan:
Update the version header to `/* mission_stream.js - v3.45.1 */`.
Add a new AudioContext function called `playResonanceCascade: function()` to the `KC.handlers.stream` object:
```javascript
playResonanceCascade: function() {
    if (!KC.audio.ctx) return;
    if (KC.audio.ctx.state === 'suspended') KC.audio.ctx.resume();
    const now = KC.audio.ctx.currentTime;
    [1,2,3,4,5,6,7,8].forEach((n, i) => {
        const osc = KC.audio.ctx.createOscillator();
        const gain = KC.audio.ctx.createGain();
        osc.type = 'sine';
        const t = now + (i * 0.28);
        const dur = 1.5 - (i * 0.12);
        osc.frequency.setValueAtTime(110 * n, t);
        gain.gain.setValueAtTime(0.12, t);
        gain.gain.exponentialRampToValueAtTime(0.01, t + dur);
        osc.connect(gain); gain.connect(KC.audio.ctx.destination);
        osc.start(t); osc.stop(t + dur);
    });
}", 23 results. Files: c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.output.txt, c:\nbs\kc\.output.txt, c:\nbs\kc\assets.js, c:\nbs\kc\kc_audio.js, c:\nbs\kc\kc_audio.js, c:\nbs\kc\kc_audio.js, c:\nbs\kc\kc_core.js, c:\nbs\kc\kc_data.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\mission_launch.js, c:\nbs\kc\mission_launch.js, c:\nbs\kc\mission_launch.js, c:\nbs\kc\mission_race.js, c:\nbs\kc\mission_stream.js, c:\nbs\kc\mission_stream.js, c:\nbs\kc\mission_stream.js, c:\nbs\kc\mission_stream.js
Thinking: The user wants to:
1. Bump the version header to v3.45.1
2. Add a new `playResonanceCascade` function to the `KC.handlers.stream` object
I need to find a good insertion point for the new function. Let me look at the existing audio function `playDropSound` which is already in the object.
Searched for text playDropSound: function( (**/mission_stream.js), 1 result. Files: c:\nbs\kc\mission_stream.js
Read mission_stream.js, lines 85 to 125
Replacing 5 lines with 24 lines in mission_stream.js
```
```
```
```
Checked mission_stream.js, no problems found
Clean. Your two blocks:
Block 1:
```
/* mission_stream.js - v3.45.1 */
```
**Block 2 — new function (inserted between
playDropSound (c:\nbs\kc\mission_stream.js:85)
and
triggerDrop (c:\nbs\kc\mission_stream.js:144)
):**
```
    // v3.45.1: Harmonic cascade played at the start of each new phase.
    playResonanceCascade: function() {
        if (!KC.audio.ctx) return;
        if (KC.audio.ctx.state === 'suspended') KC.audio.ctx.resume();
        const now = KC.audio.ctx.currentTime;
        [1,2,3,4,5,6,7,8].forEach((n, i) => {
            const osc = KC.audio.ctx.createOscillator();
            const gain = KC.audio.ctx.createGain();
            osc.type = 'sine';
            const t = now + (i * 0.28);
            const dur = 1.5 - (i * 0.12);
            osc.frequency.setValueAtTime(110 * n, t);
            gain.gain.setValueAtTime(0.12, t);
            gain.gain.exponentialRampToValueAtTime(0.01, t + dur);
            osc.connect(gain); gain.connect(KC.audio.ctx.destination);
            osc.start(t); osc.stop(t + dur);
        });
    },
```
The function is placed immediately after
playDropSound (c:\nbs\kc\mission_stream.js:85)
and before
triggerDrop (c:\nbs\kc\mission_stream.js:144)
, grouped with the other AudioContext utilities. It's ready to be called from
updatePhase (c:\nbs\kc\mission_stream.js:246)
whenever you wire in the 4000ms phase-spacer logic.