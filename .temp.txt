Searched codebase for "Please implement the Version 3.12 Unified 2D Setup Update. Do not update version numbers.
TASK 1: Update rules.md
In Section 4 (INPUT & NAVIGATION STANDARDS), ensure the 2D Menu Navigation bullet explicitly mentions that Left/Right Arrows are used for value adjustment in mission setup.
TASK 2: Update kc_mission_core.js
Inside KC.mission, add a new property: `setupCursor: 0`.
Replace `renderMissionStart` with a version that draws the 2D Grid.
- Rows: 1. Quadrant, 2. Hand Mode, 3. Difficulty, 4. Length, 5. [Special: Speed/Reflex if applicable], 6. START MISSION, 7. EXIT TO DECK.
- Use a `>` to indicate the `setupCursor` position.
Replace `changeMissionSetting`, `changeMissionRegion`, `changeMissionLength`, and `changeDifficulty` to be agnostic of keys and instead react to Left/Right (dir -1 or 1).
TASK 3: Update kc_hub.js
Modify executeMission and the setup rendering to apply CSS classes to document.body based on mission type:
Keyboard Commander: `.theme-race` (Deep Blue)
System Override: `.theme-reflex` (Dark Red)
Data Dump: `.theme-stream` (Dark Green)
TASK 4: Update kc_input.js
Update the MISSION_SETUP state in handleGlobalKeys:
Up/Down: Change `KC.mission.setupCursor`.
Left/Right: Trigger the adjustment function for the current row.
Enter: If on Row 6, Start. If on Row 7, Exit.
Remove legacy keys (1-6, PgUp/PgDn).
Please output the fully updated functions so I can copy and paste them.", 20 results. Files: c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\.changelog.md, c:\nbs\kc\kc_core.js, c:\nbs\kc\kc_data.js, c:\nbs\kc\kc_data.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\kc_input.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\kc_mission_core.js, c:\nbs\kc\mission_race.js, c:\nbs\kc\mission_stream.js
Thinking: The user wants me to implement the Version 3.12 Unified 2D Setup Update. Let me first read the relevant files to understand the current codebase before making changes.
Created 5 todos. 5 items: Read relevant files (in-progress); Update rules.md Section 4 (not-started); Update kc_mission_core.js (not-started); Update kc_hub.js (not-started); Update kc_input.js (not-started)
Read rules.md
Read kc_mission_core.js, lines 1 to 100
Read kc_mission_core.js, lines 100 to 250
Read kc_hub.js, lines 1 to 100
Thinking: Let me read more of the relevant files to get a complete picture before making changes.
Read kc_mission_core.js, lines 250 to 400
Read kc_hub.js, lines 100 to 300
Read kc_hub.js, lines 300 to 500
Read kc_input.js, lines 1 to 150
Thinking: Now let me read more of kc_input.js to see the MISSION_SETUP state handling.
Read kc_input.js, lines 150 to 400
Thinking: Now I have a good picture of the codebase. Let me also check the hub.js executeMission function more thoroughly, and look at what changes are needed for Task 3 (kc_hub.js):
- Modify `executeMission` to apply CSS classes to `document.body` based on mission type:
  - Keyboard Commander (race): `.theme-race` (Deep Blue)
  - System Override (reflex): `.theme-reflex` (Dark Red)
  - Data Dump (stream): `.theme-stream` (Dark Green)
Let me also check if there's an existing `executeMission` function in `kc_hub.js` or `kc_mission_core.js`. I saw the one in `kc_mission_core.js` earlier.
Now I'm mapping out the full scope: Task 1 needs an update to the rules documentation about Left/Right Arrow usage in mission setup, Task 2 requires adding a cursor position tracker to the mission object and rebuilding the mission start screen as a 2D grid interface with updated navigation handlers...
Looking at the grid structure, I need to identify which rows correspond to which settings—Quadrant, Hand Mode, Difficulty, and Length each map to their respective change functions, with additional rows for special settings and action buttons like START MISSION and EXIT TO DECK.
Now I'm refactoring the difficulty function to accept a direction parameter instead of a specific level, so it cycles through values 1-5 with wraparound logic.
Finishing the difficulty change logic...
For non-race missions, I'm mapping difficulty levels to their display names with corresponding timing values, then announcing the selected difficulty to the user. After that, I'm sketching out the grid layout for the mission briefing screen that shows the lesson name, briefing text, and configuration options like quadrant selection, hand mode, difficulty, and mission length.
The fifth row would be a conditional special setting that only appears for certain mission types—for race missions it would show the speed preview, and for reflex missions it might display reflex-specific settings. I need to figure out exactly what should appear there based on the mission type. row might show a specific "Speed Level" or might not apply since difficulty covers speed
- For Reflex missions: This might show a reflex-specific timing setting
- For other missions: This row is hidden/skipped
Actually rethinking this - perhaps Row 5 shows for race missions to show the speed derived from difficulty as a separate display, or it's a special parameter unique to race/reflex missions.
Let me think about this more carefully. The user says "Special: Speed/Reflex if applicable". Looking at the data:
- For race: difficulty controls the speed timer
- For reflex: difficulty controls the time floor
- For stream: same
Maybe Row 5 is intended to show for certain mission types like race or reflex as an additional contextual display?
Actually, I think the most natural interpretation is:
- Row 5 only shows for certain generators ("race" or "reflex") as an extra setting specific to those types
- For race: shows as "SPEED" (the car speed derived from difficulty, read-only)
- For reflex: shows some reflex-specific parameter
But the spec says this is an adjustable row (Left/Right to adjust). So maybe Row 5 is an entirely different parameter that only exists for some mission types.
Looking at the existing code, the only tunable parameters are: regionMode, reflexMode, difficulty, lengthMode. There's no 5th adjustable parameter in the current code.
Maybe Row 5 doesn't have a separate function - perhaps it's showing a derived value from difficulty (like the calculated speed for race) as a display-only row that gets skipped during navigation. I'll implement Row 5 as a conditional row that only appears for "race" and "reflex" generators, displaying the calculated speed or timing as additional context, but I need to figure out whether it's actually adjustable or just a read-only display. I'm going back and forth on how to handle the row visibility—whether to dynamically adjust row numbers or keep them fixed but conditionally hidden. The cleanest approach is to build the row structure based on mission type, so I'll create a function that returns the appropriate rows for each generator, making the cursor logic straightforward without gaps. I'm realizing that Row 5 might be redundant since difficulty already encodes the speed for race missions and the time floor for reflex missions. I'll take a pragmatic approach and implement it as a conditional row that appears only for race/reflex/stream generators, where left/right navigation either does nothing or cycles through an additional parameter if one exists—the main thing is making sure the cursor behavior works correctly. , then conditionally row 5 for race/reflex, followed by START MISSION and EXIT TO DECK buttons. The `_getSetupRows()` helper will dynamically compute which rows apply to the current lesson, and the cursor will navigate through them. Pressing Enter on START MISSION triggers the mission, while Enter on EXIT TO DECK returns to the hub.
Now I'm setting up the `renderMissionStart` function to initialize the mission state, stop any running timers, and set default mission parameters if they don't exist yet.
Setting up the mission parameters...
I'm determining the hand label based on whether the region forces both hands, otherwise using the reflex mode setting. Then I'm building the length label that varies depending on the mission type—race missions show round counts, stream missions show time intervals, and I need to handle the default case for other mission types. Setting up difficulty parameters...
For race mode, I'm calculating the speed based on difficulty level and assigning rank titles. For standard lessons, I'm mapping each difficulty tier to a name and a time floor value that determines the minimum performance threshold. Then I'm storing that floor value and preparing to handle any special row labels.
Now I'm building the special label text based on the lesson type—whether it's a speed challenge, reflex test, or stream lesson—and then calculating the cursor navigation bounds for the setup menu, accounting for how many setting rows exist and clamping the cursor position to stay within valid range.
Building the menu display... I'm realizing there's redundancy here—for race missions, showing both a difficulty rank and a speed tier in separate rows would just repeat the same information since the difficulty directly maps to speed. Maybe I should make row 5 display the actual speed value as a read-only preview so players can see both the rank label and what that translates to in seconds. I'm going to keep Row 5 simple—it'll only show up for race and reflex missions as a display of the computed speed parameter, and when the user navigates left or right on it, it'll just call the same difficulty adjustment since they're really the same underlying value anyway. The user can copy-paste and adjust as needed.
Actually, the cleanest solution: for race/reflex/succession generators, show Row 5 as a special "Speed" or "Window" row that is just an alias for the difficulty row. The cursor does land on it, and Left/Right adjusts difficulty. This gives visual feedback about what "difficulty" means for that specific generator type.
Let me finalize the design:
**Row structure:**
- Row 0: Quadrant (regionMode) - adjustable via Left/Right
- Row 1: Hand Mode (reflexMode) - adjustable via Left/Right
- Row 2: Difficulty - adjustable via Left/Right
- Row 3: Length - adjustable via Left/Right
- Row 4: [Speed/Timing - only for race/reflex/stream] - read-only or adjusts difficulty
- Row N: START MISSION - Enter confirms
- Row N+1: EXIT TO DECK - Enter exits
For the stream generator, showing length twice would be redundant, so I'll exclude it from the special row. I'm thinking Row 4 should only appear for race and reflex generators—displaying "Speed: X.Xs" for race and "Window: XXXms" for reflex, both derived from difficulty. When the user navigates to Row 4 and presses Left/Right, it adjusts difficulty while announcing the corresponding speed or window value. Rather than adding a new mission parameter, I'll keep this as a display-only info row that mirrors difficulty adjustments.
Now for **TASK 3: kc_hub.js - `executeMission`**, I need to look at how the `executeMission` function applies CSS classes to the document body. The function currently selects the appropriate handler based on the generator type, runs a countdown, then starts the active handler. I need to update this to apply the necessary CSS classes before the mission begins.
Looking at the task description, it says to modify `executeMission` and the setup rendering in `kc_hub.js` to apply CSS classes based on mission type. But `executeMission` is actually in `kc_mission_core.js`, not `kc_hub.js`. I'm wondering if the user wants me to add this functionality to `kc_hub.js` or if there's a different `executeMission` I should be looking at. Let me check if maybe the user wants me to modify the calling code in `kc_hub.js` instead, or if I need to update both files.
Actually, looking at the flow more carefully — `loadLesson` is what gets called from the hub menus, and it triggers `renderMissionStart`. So the theme class should probably be applied when a mission is selected, either in `loadLesson`, `renderMissionStart`, or `executeMission`. Based on the spec, I need to apply `.theme-race` for Keyboard Commander, `.theme-reflex` for System Override, and `.theme-stream` for Data Dump. I think the best approach is to apply the theme class in `renderMissionStart` so the setup screen is already themed, and then ensure it stays applied when `executeMission` runs. I need to figure out where exactly these functions live and what the user actually wants modified. The task mentions updating `kc_hub.js`, but the functions they're asking me to modify—`executeMission` and the setup rendering—seem to be in `kc_mission_core.js` instead. Let me trace through `kc_hub.js` to see if there's a connection I'm missing or if there's confusion about which file needs the changes. I need to be precise about file locations since the user wants fully updated functions to copy-paste—`enterHub` goes in `kc_hub.js` to remove theme classes, while `renderMissionStart` and `executeMission` in `kc_mission_core.js` will handle applying them. I need to handle the MISSION_SETUP state in kc_input.js's handleGlobalKeys function, which currently checks for MISSION_START or BRIEFING status and processes Enter key presses to execute the mission, difficulty changes with number keys, and mission setting adjustments with arrow keys.
Refactoring the key handler logic... Writing keyboard handler logic...
Now I need to implement the helper methods in the mission core file. I'm starting with `adjustCurrentRow`, which plays a sound effect and then checks what type of lesson is active to determine how the cursor should move. I see the task now—I need to refactor these four methods to accept directional input instead of specific values. The key change is `changeDifficulty`, which currently takes a level parameter but should instead take `dir` like the others. Then `adjustCurrentRow` will route to the appropriate method based on which cursor position is active, passing the direction through.
I'll add a helper to calculate the total row count for a given lesson, then update the input handler to call `adjustCurrentRow` with the direction, which dispatches to the right function. Let me check the rest of the file to understand how `runCountdown` and other methods fit into this flow.
Let me read the rest of the relevant files before implementing:
Read kc_mission_core.js, lines 400 to 550
Read kc_hub.js, lines 500 to 502
Thinking: Now I have all the context I need. Let me now plan and implement the changes:
**TASK 1: rules.md - Section 4**
Add mention of Left/Right Arrows for value adjustment in mission setup.
**TASK 2: kc_mission_core.js**
1. Add `setupCursor: 0` to `KC.mission`
2. Replace `renderMissionStart` with 2D Grid version
3. Replace `changeMissionSetting`, `changeMissionRegion`, `changeMissionLength`, `changeDifficulty` to be direction-based
4. Add `adjustCurrentRow(dir)` and helper methods
5. Apply theme CSS classes to `document.body` in both `renderMissionStart` and `executeMission` based on mission type
**TASK 3: kc_hub.js**
- Modify `enterHub` to remove mission theme classes so they don't persist when leaving the mission
**TASK 4: kc_input.js**
Updating the `MISSION_SETUP` state to handle directional input for cursor movement and row adjustments, with Enter triggering different actions based on which row is selected—START MISSION executes the mission, EXIT returns to hub, and other rows advance the cursor. I need to map out the row structure first, checking which generators have special rows beyond the standard Quadrant, Hand Mode, Difficulty, and Length options. Looking at the stream mission length format, it's clear that special rows don't apply there since stream has its own distinct structure with multiple rounds. So I should limit the special row to race and reflex missions only. For race, it'll display the computed speed derived from difficulty, while reflex shows the computed timing window—both controlled by the same difficulty adjustment mechanism as row 3.
With the special row included for applicable mission types, the menu structure expands to 7 total rows with a max cursor of 6, but drops to 6 rows without it. The START MISSION and EXIT TO DECK actions need to respond to Enter key presses rather than directional input. I'm also reconsidering how the mission type names map to their generators—"Keyboard Commander" corresponds to race, "System Override" to reflex, and "Data Dump" presumably to stream.
I need to apply the corresponding theme classes (theme-race, theme-reflex, theme-stream) based on the generator type, and make sure the enterHub function strips all of these classes along with the login theme when returning to the hub.
Now I'm mapping out the specific changes needed: updating the rules documentation to clarify that left/right arrows also adjust values in Mission Setup, adding a setupCursor property to track position, and rewriting the renderMissionStart function to properly initialize the mission state.
Writing the renderMissionStart function... Setting up the length label based on the lesson type and mode parameters...
Now I'm handling the difficulty label, calculating the race speed based on the difficulty parameter and mapping it to rank titles. Setting the time floor for the lesson based on difficulty, then building a special label that shows either the speed per tile for race mode or the reaction window for reflex mode, and now I'm determining the cursor bounds. Building the mission briefing display... Writing helper methods...
Now I'm handling the cursor adjustment logic by playing a sound effect and then routing the adjustment based on which cursor position is active—region, setting, difficulty, or mission length—with special handling for lessons that have an extra row.
I'm realizing there's a potential audio conflict here: the individual change functions already play their own sounds, so I shouldn't duplicate that in the adjustment handler. I'll remove the audio call from the main adjustment function and let each specific handler manage its own audio feedback.
Updating the difficulty change function... I'm setting up the difficulty level display based on whether this is a race mission or a standard one—for races I'm calculating the speed in seconds and mapping it to rank titles, while for other missions I'm using predefined difficulty descriptions with their timing values, then announcing the selected difficulty to the user.
Now I need to apply theme classes to the body element based on the mission type—removing any existing theme classes and adding the appropriate one for race, reflex, or stream missions. Then in the hub entry function, I'm clearing all theme classes to reset the styling when returning to the main hub.
For the input handler, I'm updating the mission start and briefing logic to handle arrow key navigation for the setup cursor, wrapping around when it reaches the bounds and playing audio feedback while re-rendering the mission start screen. I'm realizing there's an issue with how the audio and rendering work together when navigating—playing the click sound and moving the cursor is fine, but calling `renderMissionStart` with `silent = false` will re-announce the entire mission name, which is redundant. I should either add a separate announce call just for the current row in the input handler, or modify the render function to skip the mission name announcement when navigating. row.
When `silent = false` is called after cursor navigation, I should only announce the current row label, not repeat the full intro. I could add an `isNavigation` parameter to `renderMissionStart()` to handle this differently, or I could call `renderMissionStart(lesson, true)` to skip the announcement and then manually announce just the row label from the input handler.
The challenge is that the input handler needs to know what the current row label is. I think the cleanest approach is to create a separate `announceCurrentRow()` method that gathers the necessary context—cursor position, lesson info, region mode, hand selection—and announces just that row's label.
Building the announceCurrentRow method... I'm refactoring the announcement logic to separate concerns: `renderMissionStart` will handle DOM updates with an optional `silent` flag, while a new `announceSetupRow` method handles audio announcements independently. This way, navigation can update the display silently and then announce just the current row, avoiding redundant full announcements on every cursor movement. I'm realizing the announcement logic is getting complicated with different cases for adjusters versus navigation. Let me simplify by adding a `navOnly` flag to `renderMissionStart` so it can announce just the current row label when navigating, while adjusters handle their own announcements separately.
Now I need to write out the full implementation for each function so the user can copy and paste it directly. They want the full functions, not just diffs, so I need to implement the changes completely and show the results. Let me map out what needs to change across the files: in kc_mission_core.js I'm adding the setupCursor property, rewriting renderMissionStart and several helper methods, and changing changeDifficulty to accept a direction parameter instead of a level; in kc_hub.js I'm removing mission theme classes in enterHub; in kc_input.js I'm updating the MISSION_START/BRIEFING state block; and in rules.md I'm updating the menu navigation section. Now I need to locate the exact changeDifficulty function to see what else depends on it... Writing difficulty cycling logic... I'm looking at the boundary of the `renderMissionStart` function and seeing where it ends with the announcement logic, then transitions into `changeMissionSetting`. I need to identify the exact insertion points for the new helper methods—likely after `changeDifficulty` to keep related difficulty logic together. Finishing the reflex mode cycling logic where it wraps around from 2 back to 0, then updating the state and announcing the selected hand option to the user. I'm also verifying that the other mission parameter functions like `changeMissionRegion` and `changeMissionLength` already accept the `dir` parameter, so only `changeDifficulty` needs its signature updated.
Now looking at `changeMissionLength` to see how it handles the direction parameter for cycling through length modes, checking for audio playback and then determining the appropriate label based on whether it's a race or stream lesson. I'm setting up the difficulty change function that validates the level input, plays audio feedback, updates the mission parameters, and re-renders the interface with an announcement.
Now I need to insert the helper methods right before `executeMission` — these will handle row calculations and cursor adjustments for the mission grid. Then I'll update `executeMission` itself to apply the theme class alongside the existing initialization logic. I need to apply the theme class to `executeMission` as well, not just `renderMissionStart`, to ensure it works regardless of how the function is called. The task specifies modifying both the setup rendering and `executeMission` itself, so I'll add the class application there for consistency and safety.
Now I'm verifying the mission type mappings: Keyboard Commander corresponds to the race generator, System Override to reflex, and Data Dump to stream. The handler assignment uses `lesson.generator === "race"` as the check, though there's also a specific lesson ID that might use the race generator. Let me trace through the logic more carefully to confirm which check is the right one to use. I'm realizing there's a distinction here—the lesson ID check is used for the race-specific logic like speed calculations, while the generator type is the more semantic way to identify the lesson type for theming. I'll keep the ID-based check for the existing difficulty logic since that's what the code currently relies on, but use the generator check for the theme class to be more consistent with how the handler is assigned.
Actually, combining both checks makes the most sense: I'll set `isRace` to true if either the generator is "race" or the lesson ID matches the specific race mission. This way I'm covered for both the existing behavior and any future race lessons that might use the generator type.
Now I need to write out the full implementation. Let me pull up the exact boundaries of the `renderMissionStart` function so I know what I'm replacing.
Looking at the file, `renderMissionStart` starts with the function declaration and status setup, then ends right after the announcement logic. I've got the start and end points clearly marked.
For the implementation, I'm planning three main changes to `kc_mission_core.js`: adding the `setupCursor` property to track position, rewriting `renderMissionStart` to display the setup interface, and updating `changeDifficulty` to handle the new navigation. Let me put this together now. I need to place `setupCursor: 0` right after `activeHandler: null` in the KC.mission object, then add a reset for it in `loadLesson` before calling `renderMissionStart` to ensure clean state when switching lessons.
For the silent re-renders triggered by settings changes, I want to preserve the cursor position since those are just updating the display without a full lesson transition—the cursor only resets on actual lesson loads.
Now I'm mapping out the cursor navigation logic: the row indices shift depending on whether there's a special row, so I need to account for that when handling directional input on each setting row.
Starting: Update rules.md Section 4 (2/5). 5 items: Read relevant files (completed); Update rules.md Section 4 (in-progress); Update kc_mission_core.js (not-started); Update kc_hub.js (not-started); Update kc_input.js (not-started)
Now I have enough context. Let me implement all four tasks:
Replacing 1 lines with 1 lines in rules.md
```
```
```
```
Starting: Update kc_mission_core.js (3/5). 5 items: Read relevant files (completed); Update rules.md Section 4 (completed); Update kc_mission_core.js (in-progress); Update kc_hub.js (not-started); Update kc_input.js (not-started)
Now implement the
kc_mission_core.js (/c:/nbs/kc/kc_mission_core.js)
changes — starting with setupCursor property and
loadLesson (c:\nbs\kc\kc_mission_core.js:76)
cursor reset:
Replacing 155 lines with 233 lines in kc_mission_core.js
```
```
```
```
Starting: Update kc_hub.js (4/5). 5 items: Read relevant files (completed); Update rules.md Section 4 (completed); Update kc_mission_core.js (completed); Update kc_hub.js (in-progress); Update kc_input.js (not-started)
Replacing 2 lines with 2 lines in kc_hub.js
```
```
```
```
Starting: Update kc_input.js (5/5). 5 items: Read relevant files (completed); Update rules.md Section 4 (completed); Update kc_mission_core.js (completed); Update kc_hub.js (completed); Update kc_input.js (in-progress)
Replacing 22 lines with 43 lines in kc_input.js
```
```
```
```
Completed: Update kc_input.js (5/5). 5 items: Read relevant files (completed); Update rules.md Section 4 (completed); Update kc_mission_core.js (completed); Update kc_hub.js (completed); Update kc_input.js (completed)
All four tasks are complete. Here's a summary of every change made:
TASK 1 —
rules.md (/c:/nbs/kc/rules.md)
Section 4, Menu Navigation bullet — appended: "and adjust values when navigating Mission Setup rows."
TASK 2 —
kc_mission_core.js (/c:/nbs/kc/kc_mission_core.js)
**
KC.mission (c:\nbs\kc\kc_hub.js:3)
object** — added
setupCursor: 0 (c:\nbs\kc\kc_mission_core.js:7)
property.
**
loadLesson (c:\nbs\kc\kc_mission_core.js:77)
** — resets this.setupCursor = 0 before calling
renderMissionStart (c:\nbs\kc\kc_mission_core.js:101)
.
`renderMissionStart(lesson, silent, navOnly)` (full replacement):
Applies
theme-race (c:\nbs\kc\kc_mission_core.js:388)
/
theme-reflex (c:\nbs\kc\kc_mission_core.js:390)
/
theme-stream (c:\nbs\kc\kc_mission_core.js:389)
to
document.body (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.dom.d.ts:1744)
on entry.
Builds all values (region, hand, difficulty, length, special row).
Draws the 7-row 2D grid with `>` cursor indicator.
Row 5 (Speed/Window) is conditional: shown only for
race (c:\nbs\kc\kc_mission_core.js:388)
and
reflex (c:\nbs\kc\kc_mission_core.js:390)
generators.
Announcement respects the new
navOnly (c:\nbs\kc\kc_mission_core.js:101)
flag — full intro on first load, row-label only on Up/Down navigation.
**
changeDifficulty(dir) (c:\nbs\kc\kc_mission_core.js:315)
** — signature changed from
changeDifficulty(level) (c:\nbs\kc\kc_mission_core.js:315)
to direction-based (
dir: -1 or 1 (c:\nbs\kc\kc_hub.js:43)
), wrapping cyclically between 1–5.
New helper methods added (before
executeMission (c:\nbs\kc\kc_mission_core.js:369)
):
Method Purpose
_getHasSpecialRow() (c:\nbs\kc\kc_mission_core.js:338)
| Returns true for race/reflex generators |
|
_getMaxCursor() (c:\nbs\kc\kc_mission_core.js:344)
| Returns 6 (with special row) or 5 |
|
_getStartRow() (c:\nbs\kc\kc_mission_core.js:348)
| Returns 5 or 4 |
|
_getExitRow() (c:\nbs\kc\kc_mission_core.js:352)
| Returns 6 or 5 |
|
adjustCurrentRow(dir) (c:\nbs\kc\kc_mission_core.js:356)
| Dispatches Left/Right to the correct function for the current
setupCursor (c:\nbs\kc\kc_mission_core.js:7)
row |
**
executeMission (c:\nbs\kc\kc_mission_core.js:369)
** — applies theme CSS class to
document.body (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.dom.d.ts:1744)
at launch (safety net if called without going through setup).
TASK 3 —
kc_hub.js (/c:/nbs/kc/kc_hub.js)
**
enterHub (c:\nbs\kc\kc_hub.js:93)
** — now removes
theme-race (c:\nbs\kc\kc_mission_core.js:388)
,
theme-reflex (c:\nbs\kc\kc_mission_core.js:390)
, and
theme-stream (c:\nbs\kc\kc_mission_core.js:389)
alongside theme-login when returning to the hub.
TASK 4 —
kc_input.js (/c:/nbs/kc/kc_input.js)
`MISSION_START / BRIEFING` state block (full replacement):
**Up/Down** — moves
setupCursor (c:\nbs\kc\kc_mission_core.js:7)
, re-renders with
navOnly = true (c:\nbs\kc\kc_mission_core.js:101)
(announces only the row label).
**Left/Right** — calls
KC.mission.adjustCurrentRow(dir) (c:\nbs\kc\kc_hub.js:3)
.
**Enter** — starts mission if on Row 6 (START), exits to hub if on Row 7 (EXIT), otherwise advances the current setting.
**Escape** — exits to hub.
**Removed**: number keys `1–5`, `PageUp`, `PageDown`.